# План разработки компонента Secrets Vault

## Обзор

Этот документ описывает план доработки компонента Secrets Vault для соответствия реальной архитектуре HashiCorp Vault и улучшения симулятивности, UI/UX.

**Важно**: Каждый компонент уникален. Не копируем слепо реализацию других компонентов, но изучаем паттерны для понимания архитектуры системы.

---

## 1. Подготовка к разработке

### 1.1 Изучить архитектуру системы

**Файлы для изучения:**
- `src/core/EmulationEngine.ts` - понять как инициализируются движки, метод `initialize()`, паттерны создания специализированных движков
- `src/core/DataFlowEngine.ts` - понять как обрабатываются потоки данных, метод `registerHandler()`, паттерны обработки сообщений
- `src/services/connection/ServiceDiscovery.ts` - понять как разрешаются имена/порты для компонентов
- `src/services/connection/rules/` - изучить примеры connection rules (keycloakRules.ts, firewallRules.ts)

### 1.2 Изучить похожие компоненты

**Компоненты для изучения паттернов:**
- `Keycloak` (security) - изучить KeycloakEmulationEngine, KeycloakConfigAdvanced, keycloakRules.ts
- `Prometheus` (observability) - изучить как реализованы pull-based операции
- `PostgreSQL` (data) - изучить как реализованы операции с данными и версионирование

**Что изучить:**
- Паттерны инициализации движков
- Паттерны обработки операций
- Паттерны расчета метрик
- Паттерны connection rules
- Паттерны UI/UX для сложных конфигураций

### 1.3 Изучить реальную архитектуру HashiCorp Vault

**Ключевые концепции:**
- **Seal/Unseal механизм** - Vault стартует в sealed состоянии, требует unsealing
- **Storage Backend** - persistent storage (Consul, etcd, file, S3, etc.)
- **Barrier Layer** - encryption layer для всех данных
- **Secrets Engines** - KV v1/v2, Transit, PKI, Database, AWS, Azure
- **Auth Methods** - Token, AppRole, LDAP, AWS, OIDC, etc.
- **Policies** - HCL-based access control
- **Audit Devices** - логирование всех операций
- **Namespaces** (Enterprise) - multi-tenancy
- **KV v1 vs v2** - v2 поддерживает versioning, metadata, CAS (check-and-set)
- **Dynamic Secrets** - автоматическая генерация и ротация
- **Secret Rotation** - автоматическая смена секретов

**Документация:**
- https://developer.hashicorp.com/vault/docs/internals/architecture
- https://developer.hashicorp.com/vault/api-docs
- https://developer.hashicorp.com/vault/docs/secrets

### 1.4 Проверить существующие файлы

**Текущие файлы:**
- ✅ `src/core/VaultEmulationEngine.ts` - базовый движок существует
- ✅ `src/components/config/security/SecretsVaultConfigAdvanced.tsx` - UI существует
- ✅ Интеграция в `EmulationEngine.ts` - есть
- ✅ Интеграция в `DataFlowEngine.ts` - есть
- ❌ `src/services/connection/rules/vaultRules.ts` - **ОТСУТСТВУЕТ**
- ❌ Регистрация в `src/services/connection/rules/index.ts` - **ОТСУТСТВУЕТ**

---

## 2. Выявленные проблемы

### 2.1 Симулятивность

#### Критические проблемы:
1. **Нет Seal/Unseal механизма**
   - Vault должен стартовать в sealed состоянии
   - Требуется unsealing перед операциями
   - Влияет на метрики и доступность

2. **Нет различий между KV v1 и v2**
   - KV v2 поддерживает versioning, metadata, CAS
   - KV v1 - простой key-value без версий
   - Текущая реализация не различает версии

3. **Нет List операций**
   - Метрика `listRequestsTotal` есть, но метода `processListRequest()` нет
   - Нужна поддержка `LIST /v1/secret/metadata/` для KV v2
   - Нужна поддержка `LIST /v1/secret/` для KV v1

4. **Нет версионирования секретов (KV v2)**
   - Нет истории версий секрета
   - Нет чтения конкретной версии
   - Нет CAS (check-and-set) операций

5. **Нет Storage Backend симуляции**
   - Нет выбора storage backend (Consul, etcd, file, S3)
   - Нет влияния storage на производительность

6. **Нет Audit Devices**
   - Нет управления audit devices
   - Нет влияния на метрики

7. **Нет Dynamic Secrets**
   - Нет генерации динамических секретов (database, AWS, etc.)
   - Нет автоматической ротации

8. **Нет PKI Engine операций**
   - Нет генерации сертификатов
   - Нет управления CA

9. **Нет Database Secrets Engine**
   - Нет генерации временных credentials для БД

#### Средние проблемы:
10. **Нет Namespaces (Enterprise)**
    - Не критично для базовой симуляции

11. **Нет Secret Rotation**
    - Не критично, но важно для реалистичности

### 2.2 UI/UX

#### Критические проблемы:
1. **Хардкод дефолтных значений**
   - Строки 109-148 в `SecretsVaultConfigAdvanced.tsx` - хардкод дефолтных secrets, engines, policies
   - Должны браться из конфига или быть пустыми

2. **Нет валидации путей секретов**
   - Нет проверки формата путей (должны начинаться с engine name)
   - Нет проверки на дубликаты

3. **Нет поддержки версионирования в UI (KV v2)**
   - Нет отображения истории версий
   - Нет возможности читать конкретную версию
   - Нет CAS операций

4. **Нет управления Seal/Unseal**
   - Нет UI для seal/unseal операций
   - Нет отображения состояния (sealed/unsealed)

5. **Нет управления Storage Backend**
   - Нет выбора storage backend
   - Нет конфигурации storage

6. **Нет управления Audit Devices**
   - Нет UI для audit devices

7. **Нет поддержки List операций в UI**
   - Нет отображения списка секретов по path

#### Средние проблемы:
8. **Нет поддержки Dynamic Secrets в UI**
9. **Нет поддержки PKI Engine в UI**
10. **Нет поддержки Database Secrets Engine в UI**

### 2.3 Connection Rules

#### Критические проблемы:
1. **Отсутствуют Connection Rules**
   - Нет файла `src/services/connection/rules/vaultRules.ts`
   - Нет регистрации в `index.ts`
   - Компоненты не могут автоматически настраиваться при подключении к Vault

---

## 3. План реализации

### Этап 1: Connection Rules (Приоритет: ВЫСОКИЙ)

**Цель**: Реализовать connection rules для автоматической настройки компонентов при подключении к Vault.

**Задачи:**
1. Создать `src/services/connection/rules/vaultRules.ts`
2. Реализовать правило для подключения к Vault:
   - Автоматическая настройка адреса Vault в source компоненте
   - Автоматическая настройка токена (если есть)
   - Автоматическая настройка пути к секретам
3. Зарегистрировать правило в `src/services/connection/rules/index.ts`

**Примеры использования:**
- REST API → Vault: настройка Vault адреса в REST API конфиге
- Application → Vault: настройка credentials для доступа к Vault
- Database → Vault: настройка получения credentials из Vault

**Файлы для изучения:**
- `src/services/connection/rules/keycloakRules.ts` - пример security компонента
- `src/services/connection/rules/databaseRules.ts` - пример настройки credentials

---

### Этап 2: Seal/Unseal механизм (Приоритет: ВЫСОКИЙ)

**Цель**: Реализовать seal/unseal механизм для реалистичной симуляции.

**Задачи:**

#### 2.1 VaultEmulationEngine
1. Добавить состояние `sealed: boolean` в `VaultEmulationConfig`
2. Добавить методы:
   - `seal()` - запечатать Vault
   - `unseal(shards: string[])` - распечатать Vault (Shamir's Secret Sharing)
   - `isSealed(): boolean` - проверить состояние
3. Модифицировать все операции (read, write, delete, auth, encrypt, decrypt):
   - Проверять `sealed` состояние
   - Возвращать ошибку если sealed
4. Добавить метрики:
   - `vault_sealed: boolean`
   - `vault_unseal_attempts_total: number`
   - `vault_unseal_success_total: number`

#### 2.2 UI
1. Добавить вкладку "Status" в `SecretsVaultConfigAdvanced.tsx`
2. Отображать состояние (Sealed/Unsealed) с визуальным индикатором
3. Добавить кнопки:
   - "Seal Vault" - запечатать
   - "Unseal Vault" - диалог для ввода unseal keys (shards)
4. Добавить настройки:
   - Количество shards для unseal (по умолчанию 3 из 5)
   - Auto-unseal (через KMS/HSM) - опционально

**Реалистичность:**
- Vault стартует в sealed состоянии
- Требуется unsealing перед любыми операциями
- Unsealing требует определенное количество shards (Shamir's Secret Sharing)

---

### Этап 3: KV v1 vs v2 различия (Приоритет: ВЫСОКИЙ)

**Цель**: Реализовать правильные различия между KV v1 и v2.

**Задачи:**

#### 3.1 VaultEmulationEngine
1. Модифицировать структуру секретов:
   - Для KV v1: простой key-value
   - Для KV v2: версионирование, metadata, CAS
2. Добавить интерфейс для KV v2:
   ```typescript
   interface KV2SecretVersion {
     version: number;
     created: string;
     deleted?: boolean;
     destroyed?: boolean;
   }
   
   interface KV2Secret {
     path: string;
     versions: Map<number, { data: Record<string, string>, metadata: KV2SecretVersion }>;
     currentVersion: number;
     casRequired?: boolean;
   }
   ```
3. Модифицировать операции:
   - `processReadRequest()` - поддержка чтения конкретной версии для KV v2
   - `processWriteRequest()` - поддержка CAS для KV v2, автоматическое версионирование
   - `processDeleteRequest()` - поддержка soft delete для KV v2
   - Добавить `processListRequest()` - список секретов по path
4. Добавить методы:
   - `processReadVersion(path: string, version: number)` - чтение конкретной версии
   - `processUndelete(path: string, versions: number[])` - восстановление версий
   - `processDestroy(path: string, versions: number[])` - уничтожение версий
   - `processListSecrets(path: string)` - список секретов

#### 3.2 UI
1. Модифицировать отображение секретов:
   - Для KV v2: показывать историю версий
   - Для KV v1: простой key-value
2. Добавить UI для:
   - Просмотра истории версий (KV v2)
   - Чтения конкретной версии
   - CAS операций (check-and-set)
   - List операций (показать все секреты по path)

**Реалистичность:**
- KV v1: `/v1/secret/` - простой key-value, нет версий
- KV v2: `/v1/secret/data/` - версионирование, metadata, CAS
- KV v2: `/v1/secret/metadata/` - управление metadata
- KV v2: `/v1/secret/delete/` - soft delete
- KV v2: `/v1/secret/undelete/` - восстановление
- KV v2: `/v1/secret/destroy/` - уничтожение версий

---

### Этап 4: List операции (Приоритет: СРЕДНИЙ)

**Цель**: Реализовать List операции для просмотра секретов.

**Задачи:**

#### 4.1 VaultEmulationEngine
1. Добавить метод `processListRequest(path: string, token?: string)`:
   - Возвращает список секретов по path
   - Для KV v1: список ключей
   - Для KV v2: список paths с metadata
2. Обновить метрики `listRequestsTotal`

#### 4.2 DataFlowEngine
1. Добавить обработку операции `list` в `createVaultHandler()`

#### 4.3 UI
1. Добавить кнопку "List Secrets" в секции Secrets
2. Показывать список секретов по выбранному path
3. Поддержка навигации по иерархии paths

---

### Этап 5: Storage Backend (Приоритет: СРЕДНИЙ)

**Цель**: Реализовать симуляцию storage backend.

**Задачи:**

#### 5.1 VaultEmulationConfig
1. Добавить поле `storageBackend: 'consul' | 'etcd' | 'file' | 's3' | 'inmem'`
2. Добавить конфигурацию storage (зависит от типа)

#### 5.2 VaultEmulationEngine
1. Добавить влияние storage на латентность:
   - Consul: низкая латентность, высокая доступность
   - etcd: средняя латентность
   - File: высокая латентность, низкая доступность
   - S3: высокая латентность, высокая доступность
   - Inmem: очень низкая латентность, нет персистентности

#### 5.3 UI
1. Добавить выбор storage backend в Settings
2. Показывать влияние на метрики

---

### Этап 6: Audit Devices (Приоритет: НИЗКИЙ)

**Цель**: Реализовать управление audit devices.

**Задачи:**

#### 6.1 VaultEmulationConfig
1. Добавить поле `auditDevices: Array<{ name: string, type: string, enabled: boolean }>`

#### 6.2 VaultEmulationEngine
1. Добавить влияние audit на латентность (небольшое увеличение)
2. Добавить метрики audit операций

#### 6.3 UI
1. Добавить вкладку "Audit" для управления audit devices

---

### Этап 7: Улучшение UI/UX (Приоритет: ВЫСОКИЙ)

**Цель**: Убрать хардкод, улучшить валидацию, добавить недостающий функционал.

**Задачи:**

#### 7.1 Убрать хардкод
1. Удалить хардкод дефолтных значений в `SecretsVaultConfigAdvanced.tsx` (строки 109-148)
2. Использовать значения из конфига или пустые массивы
3. Использовать defaults из `profiles.ts` если конфиг пустой

#### 7.2 Валидация
1. Добавить валидацию путей секретов:
   - Должны начинаться с engine name (например, `secret/`)
   - Не должны содержать недопустимые символы
   - Не должны быть дубликатами
2. Добавить валидацию engine names:
   - Должны заканчиваться на `/`
   - Не должны быть дубликатами
3. Добавить валидацию policy rules (HCL syntax) - опционально

#### 7.3 Улучшения UI
1. Добавить визуальные индикаторы:
   - Состояние Vault (sealed/unsealed)
   - Тип engine (KV v1/v2)
   - Версии секретов (KV v2)
2. Улучшить отображение секретов:
   - Группировка по path
   - Поиск и фильтрация
   - Сортировка
3. Добавить контекстные подсказки и документацию

---

### Этап 8: Dynamic Secrets (Приоритет: НИЗКИЙ)

**Цель**: Реализовать поддержку dynamic secrets.

**Задачи:**

#### 8.1 VaultEmulationEngine
1. Добавить поддержку Database Secrets Engine:
   - Генерация временных credentials
   - Автоматическая ротация
   - TTL для credentials
2. Добавить поддержку AWS Secrets Engine:
   - Генерация временных AWS credentials
3. Добавить метрики:
   - `vault_dynamic_secrets_generated_total`
   - `vault_dynamic_secrets_active`

#### 8.2 UI
1. Добавить UI для настройки dynamic secrets engines
2. Показывать активные dynamic secrets
3. Показывать TTL и время истечения

---

### Этап 9: PKI Engine (Приоритет: НИЗКИЙ)

**Цель**: Реализовать базовую поддержку PKI Engine.

**Задачи:**

#### 9.1 VaultEmulationEngine
1. Добавить методы:
   - `processGenerateCertificate()` - генерация сертификата
   - `processRevokeCertificate()` - отзыв сертификата
2. Добавить метрики:
   - `vault_pki_certificates_generated_total`
   - `vault_pki_certificates_revoked_total`

#### 9.2 UI
1. Добавить UI для PKI операций (если enablePKI = true)

---

## 4. Порядок реализации

### Фаза 1: Критические улучшения (1-2 недели)
1. ✅ **ЗАВЕРШЕНО** Этап 1: Connection Rules
   - ✅ Создан `src/services/connection/rules/vaultRules.ts`
   - ✅ Зарегистрировано в `src/services/connection/rules/index.ts`
   - ✅ Поддержка автоматической настройки для REST API, API Gateway, Kong, Databases
2. ✅ **ЗАВЕРШЕНО** Этап 2: Seal/Unseal механизм
   - ✅ Добавлено состояние `sealed` в `VaultEmulationConfig`
   - ✅ Реализованы методы `seal()`, `unseal()`, `isSealed()`, `getSealState()`
   - ✅ Все операции проверяют seal состояние
   - ✅ Добавлены метрики `vaultSealed`, `unsealAttemptsTotal`, `unsealSuccessTotal`
   - ✅ UI для Seal/Unseal в вкладке Status
3. ✅ **ЗАВЕРШЕНО** Этап 3: KV v1 vs v2 различия
   - ✅ Реализована структура `KV2Secret` с версионированием
   - ✅ Поддержка чтения конкретной версии для KV v2
   - ✅ Поддержка CAS (check-and-set) операций для KV v2
   - ✅ Soft delete для KV v2, hard delete для KV v1
   - ✅ Автоматическое версионирование при записи
4. ✅ **ЗАВЕРШЕНО** Этап 7: Улучшение UI/UX (убрать хардкод, валидация)
   - ✅ Удален весь хардкод дефолтных значений из `SecretsVaultConfigAdvanced.tsx`
   - ✅ Добавлена валидация путей секретов (должны начинаться с engine name)
   - ✅ Добавлена валидация engine names (должны заканчиваться на /)
   - ✅ Проверка на дубликаты
   - ✅ Добавлена вкладка Status с отображением состояния Vault

### Фаза 2: Важные улучшения (1 неделя)
5. ✅ **ЗАВЕРШЕНО** Этап 4: List операции
   - ✅ Реализован метод `processListRequest()` в `VaultEmulationEngine`
   - ✅ Поддержка List для KV v1 и v2
   - ✅ Интеграция в `DataFlowEngine` (операция `list`)
   - ✅ Метрика `listRequestsTotal` обновляется
6. ✅ **ЗАВЕРШЕНО** Этап 5: Storage Backend
   - ✅ Добавлен `StorageBackendConfig` в конфигурацию
   - ✅ Поддержка типов: consul, etcd, file, s3, inmem
   - ✅ Влияние storage на латентность операций
   - ✅ UI для выбора storage backend в Settings

### Фаза 3: Дополнительные улучшения (по необходимости)
7. ⏳ **НЕ РЕАЛИЗОВАНО** Этап 6: Audit Devices
   - ❌ Не реализовано (низкий приоритет)
   - **Статус**: Не требуется для базовой симуляции. Может быть реализовано в будущем при необходимости.
8. ⏳ **НЕ РЕАЛИЗОВАНО** Этап 8: Dynamic Secrets
   - ❌ Не реализовано (низкий приоритет)
   - **Статус**: Не требуется для базовой симуляции. Может быть реализовано в будущем при необходимости.
9. ⏳ **НЕ РЕАЛИЗОВАНО** Этап 9: PKI Engine
   - ❌ Не реализовано (низкий приоритет)
   - **Статус**: Не требуется для базовой симуляции. Может быть реализовано в будущем при необходимости.

---

## ✅ ИТОГОВЫЙ СТАТУС РЕАЛИЗАЦИИ

**Дата последнего обновления**: 2026-01-26

### ✅ ЗАВЕРШЕНО (Все критические и важные этапы):
1. ✅ **Этап 1: Connection Rules** - Полностью реализовано
2. ✅ **Этап 2: Seal/Unseal механизм** - Полностью реализовано
3. ✅ **Этап 3: KV v1 vs v2 различия** - Полностью реализовано
4. ✅ **Этап 4: List операции** - Полностью реализовано
5. ✅ **Этап 5: Storage Backend** - Полностью реализовано
6. ✅ **Этап 7: Улучшение UI/UX** - Полностью реализовано (удален хардкод, добавлена валидация)

### ⏳ НЕ РЕАЛИЗОВАНО (Низкий приоритет, не требуется для базовой симуляции):
7. ⏳ **Этап 6: Audit Devices** - Не реализовано (низкий приоритет)
8. ⏳ **Этап 8: Dynamic Secrets** - Не реализовано (низкий приоритет)
9. ⏳ **Этап 9: PKI Engine** - Не реализовано (низкий приоритет)

### 📝 ЗАКЛЮЧЕНИЕ:
**Все критические и важные этапы разработки компонента Vault завершены.** Компонент полностью функционален для базовой симуляции работы HashiCorp Vault с поддержкой:
- Connection Rules для автоматической настройки
- Seal/Unseal механизма
- Различий между KV v1 и v2
- List операций
- Storage Backend симуляции
- Полного отсутствия хардкода (все значения из конфига)

Низкоприоритетные этапы (Audit Devices, Dynamic Secrets, PKI Engine) могут быть реализованы в будущем при необходимости, но не требуются для базовой симуляции.

---

## 5. Тестирование

### 5.1 Unit тесты
- Тесты для VaultEmulationEngine методов
- Тесты для connection rules
- Тесты для валидации

### 5.2 Integration тесты
- Тесты интеграции с EmulationEngine
- Тесты интеграции с DataFlowEngine
- Тесты connection rules

### 5.3 Ручное тестирование
- Проверка UI/UX
- Проверка симуляции в реальном времени
- Проверка метрик

---

## 6. Документация

### 6.1 Комментарии в коде
- JSDoc для всех публичных методов
- Комментарии для сложной логики

### 6.2 README
- Описание компонента
- Примеры использования
- Описание конфигурации

---

## 7. Чеклист перед завершением

- [x] ✅ Все connection rules реализованы и зарегистрированы
- [x] ✅ Seal/Unseal механизм работает корректно
- [x] ✅ KV v1 и v2 различия реализованы
- [x] ✅ List операции работают
- [x] ✅ Storage Backend влияет на метрики
- [x] ✅ Хардкод удален из UI
- [x] ✅ Валидация добавлена
- [x] ✅ UI улучшен (добавлена вкладка Status, Seal/Unseal UI)
- [x] ✅ Метрики корректны (добавлены seal метрики)
- [x] ✅ Интеграция с EmulationEngine работает
- [x] ✅ Интеграция с DataFlowEngine работает (поддержка list, version, cas)
- [x] ✅ Код соответствует правилам курсора (безопасный доступ к данным)
- [x] ✅ Нет скриптованности и хардкода (все значения из конфига)
- [x] ✅ Соответствует реальной архитектуре HashiCorp Vault

## 10. Статус реализации (обновлено)

**Дата завершения**: 2026-01-26

**Реализовано**:
- ✅ Connection Rules для автоматической настройки компонентов
- ✅ Seal/Unseal механизм с поддержкой Shamir's Secret Sharing
- ✅ Различия между KV v1 и v2 (версионирование, CAS, soft delete)
- ✅ List операции для просмотра секретов
- ✅ Storage Backend симуляция с влиянием на латентность
- ✅ Удален весь хардкод из UI
- ✅ Добавлена валидация путей и engine names
- ✅ UI для управления Seal/Unseal состоянием

**Не реализовано (низкий приоритет)**:
- ⏳ Audit Devices
- ⏳ Dynamic Secrets (Database, AWS engines)
- ⏳ PKI Engine операции

**Измененные файлы**:
- ✅ `src/services/connection/rules/vaultRules.ts` - **СОЗДАН**
- ✅ `src/services/connection/rules/index.ts` - **ОБНОВЛЕН**
- ✅ `src/core/VaultEmulationEngine.ts` - **ОБНОВЛЕН** (Seal/Unseal, KV v2, List, Storage)
- ✅ `src/core/DataFlowEngine.ts` - **ОБНОВЛЕН** (поддержка list, version, cas)
- ✅ `src/components/config/security/SecretsVaultConfigAdvanced.tsx` - **ОБНОВЛЕН** (удален хардкод, добавлен Status UI)

---

## 8. Примечания

- **Не копировать слепо** другие компоненты - каждый уникален
- **Изучать паттерны** из похожих компонентов
- **Следовать реальной архитектуре** HashiCorp Vault
- **Избегать хардкода** - все должно быть конфигурируемо
- **Улучшать постепенно** - не пытаться реализовать все сразу
- **Тестировать на каждом этапе** - не накапливать технический долг

---

## 9. Ссылки

- HashiCorp Vault Architecture: https://developer.hashicorp.com/vault/docs/internals/architecture
- HashiCorp Vault API: https://developer.hashicorp.com/vault/api-docs
- HashiCorp Vault Secrets Engines: https://developer.hashicorp.com/vault/docs/secrets
- KV Secrets Engine: https://developer.hashicorp.com/vault/docs/secrets/kv
- KV v1 vs v2: https://developer.hashicorp.com/vault/docs/secrets/kv/kv-v2
