# План разработки компонента Prometheus

## Архитектурная модель: Pull-based система

### ⚠️ КРИТИЧЕСКИ ВАЖНО понимать:

**Prometheus работает по PULL-модели, а не PUSH:**

1. **Компоненты НЕ подключаются к Prometheus**
   - Компоненты просто экспортируют метрики на HTTP endpoint (обычно `/metrics`)
   - Они пассивно ждут запросов от Prometheus

2. **Prometheus сам инициирует подключения**
   - Prometheus периодически делает HTTP GET запросы к targets
   - Он сам находит targets через Service Discovery или static configs
   - Это однонаправленный поток: Prometheus → Component (scrape request)

3. **Connection Rules в симуляторе**
   - Связь Component → Prometheus в UI - это **конфигурация**, а не сетевое подключение
   - Она добавляет target в `scrape_configs` Prometheus
   - В реальности Prometheus сам находит компоненты через SD или static configs

4. **Service Discovery - основа работы Prometheus**
   - Kubernetes SD: Prometheus опрашивает Kubernetes API для поиска Pods/Services
   - Consul SD: Prometheus опрашивает Consul для списка сервисов
   - Static configs: список `host:port` вручную
   - File SD: Prometheus читает файлы с targets

5. **Метрики компонентов**
   - Каждый компонент должен иметь виртуальный metrics port
   - Экспортировать метрики в Prometheus format на `/metrics`
   - Prometheus скрейпит этот endpoint периодически

### Последствия для реализации:

- ✅ Service Discovery должен быть **приоритетом #1** - без него Prometheus не может найти targets
- ✅ Scraping должен симулировать **HTTP GET запросы** от Prometheus к компонентам
- ✅ Connection rules правильны, но это **конфигурация**, а не реальное подключение
- ✅ Компоненты должны экспортировать метрики **независимо** от Prometheus

---

## Анализ текущего состояния

### ✅ Что уже реализовано хорошо:
1. **Базовый Emulation Engine** (`PrometheusEmulationEngine.ts`) - симуляция scraping
2. **Конфигурация** (`PrometheusConfigAdvanced.tsx`) - UI для настройки scrape_configs, alerting rules, recording rules
3. **Metrics Exporter** (`PrometheusMetricsExporter.ts`) - экспорт в Prometheus format
4. **Connection Rules** (`prometheusRules.ts`) - автоматическое добавление targets при создании связей (это конфигурация, не подключение)
5. **Миграция конфигов** - поддержка старой и новой структуры

### ⚠️ Ключевые моменты для понимания:

**Как компоненты взаимодействуют с Prometheus в симуляторе:**

1. **Компоненты экспортируют метрики**
   - Каждый компонент должен иметь виртуальный metrics port (например, 9100, 9187, 9121)
   - Экспортирует метрики в Prometheus format на `/metrics` endpoint
   - Это происходит **независимо** от наличия Prometheus на canvas

2. **Prometheus находит targets**
   - Через Service Discovery (Kubernetes, Consul, DNS, File)
   - Через static configs (список `host:port`)
   - Через connection rules в UI (добавляет static config)

3. **Prometheus скрейпит метрики**
   - Периодически делает HTTP GET к `/metrics` endpoints
   - Компоненты отвечают на запросы метриками
   - Это pull-based: Prometheus инициирует, компоненты отвечают

4. **Connection rules в UI**
   - Связь Component → Prometheus в UI добавляет target в `scrape_configs`
   - Это **конфигурация**, а не реальное сетевое подключение
   - В реальности Prometheus сам находит targets через SD

### ⚠️ Проблемы и области для улучшения:

#### 1. **Service Discovery - неполная реализация**
- ❌ Только базовая структура в конфиге, нет реальной логики discovery
- ❌ Нет поддержки Kubernetes SD, Consul SD, DNS SD, File SD
- ❌ Нет динамического обновления targets при изменении сервисов

#### 2. **Scraping логика - упрощенная**
- ⚠️ `findNodeByEndpoint` использует простой поиск по hostname/port, не учитывает реальные механизмы DNS resolution
- ⚠️ Нет поддержки HTTP/HTTPS схем, authentication (basic auth, bearer token, TLS)
- ⚠️ Нет retry логики при ошибках scraping
- ⚠️ Нет поддержки relabeling (важно для реального Prometheus)
- ⚠️ **Важно**: Scraping должен симулировать HTTP GET запросы от Prometheus к компонентам (pull-based)

#### 3. **Метрики Prometheus - неполные**
- ❌ Нет метрик самого Prometheus (prometheus_tsdb_*, prometheus_config_*, prometheus_target_*)
- ❌ Нет метрик для каждого target (up, scrape_duration_seconds, scrape_samples_scraped)
- ❌ Нет метрик для alerting (prometheus_notifications_*)

#### 4. **Alerting - базовая реализация**
- ⚠️ Есть структура для alerting rules, но нет реальной evaluation engine
- ❌ Нет интеграции с Alertmanager (только конфиг URL)
- ❌ Нет симуляции отправки алертов

#### 5. **Remote Write - не реализовано**
- ❌ Есть конфиг для remote_write, но нет логики отправки метрик
- ❌ Нет поддержки различных backends (Cortex, Thanos, etc.)

#### 6. **Storage - упрощенная симуляция**
- ⚠️ Нет реальной симуляции TSDB (time-series database)
- ❌ Нет метрик retention, compaction, WAL
- ❌ Нет симуляции использования диска

#### 7. **UI/UX - можно улучшить**
- ⚠️ Нет визуализации targets status в реальном времени
- ⚠️ Нет preview Prometheus UI (как в реальном Prometheus)
- ⚠️ Нет валидации PromQL выражений в alerting rules

---

## План реализации (по приоритетам)

### Фаза 1: Улучшение Service Discovery (КРИТИЧНО - ОСНОВА РАБОТЫ PROMETHEUS)

**Цель**: Реализовать полноценную поддержку Service Discovery механизмов, как в реальном Prometheus.

**⚠️ Почему это критично:**
- В реальности Prometheus **сам находит** targets через Service Discovery
- Без SD Prometheus не знает, какие компоненты скрейпить
- Connection rules в UI - это просто удобный способ добавить static config, но в реальности используется SD

#### 1.1. Kubernetes Service Discovery ✅ ВЫПОЛНЕНО
**Файлы для изменения:**
- ✅ `src/core/serviceDiscovery/KubernetesSD.ts` - создан новый модуль Kubernetes SD
- ✅ `src/core/PrometheusEmulationEngine.ts` - интегрирована логика Kubernetes SD
- ⚠️ `src/components/config/observability/PrometheusConfigAdvanced.tsx` - UI для Kubernetes SD config (частично - есть структура, но нужно улучшить)

**Задачи:**
1. ✅ Реализовать `kubernetes_sd_configs` парсинг и обработку
2. ✅ Интегрироваться с Kubernetes компонентом (если есть на canvas)
3. ✅ **Периодически опрашивать Kubernetes API** для получения списка ресурсов (симуляция)
4. ✅ Автоматически обнаруживать Pods/Services с annotations `prometheus.io/scrape=true`
5. ✅ Динамически обновлять targets при изменении Kubernetes ресурсов
6. ✅ Поддержка различных roles: `pod`, `service`, `endpoints`, `ingress`, `node`
7. ✅ **Симулировать реальное поведение**: Prometheus опрашивает Kubernetes API каждые N секунд

**Реализовано:**
- Создан класс `KubernetesSD` с поддержкой всех основных roles
- Интеграция с `KubernetesEmulationEngine` для получения реальных ресурсов
- Автоматическое обнаружение Pods с `prometheus.io/scrape=true` annotation
- Поддержка фильтрации по namespace и selectors
- Кэширование targets с периодическим обновлением (30 секунд)
- Преобразование Kubernetes мета-меток в Prometheus labels

**Реалистичность:**
- Использовать реальную структуру Kubernetes SD config из Prometheus
- Симулировать API Kubernetes для получения списка ресурсов
- Учитывать namespace, label selectors, port filtering
- **Важно**: Prometheus сам инициирует запросы к Kubernetes API, а не ждет событий

#### 1.2. Consul Service Discovery ✅ ВЫПОЛНЕНО
**Файлы для изменения:**
- ✅ `src/core/serviceDiscovery/ConsulSD.ts` - создан новый модуль Consul SD
- ✅ `src/core/PrometheusEmulationEngine.ts` - интегрирована логика Consul SD
- ⚠️ `src/components/config/observability/PrometheusConfigAdvanced.tsx` - UI для Consul SD config (частично - есть структура, но нужно улучшить)

**Задачи:**
1. ✅ Реализовать `consul_sd_configs` парсинг
2. ✅ Интегрироваться с Consul компонентом (если есть на canvas)
3. ✅ **Периодически опрашивать Consul API** для получения списка сервисов (симуляция)
4. ✅ Запрашивать список сервисов из Consul через HTTP API (симуляция через поиск компонентов на canvas)
5. ✅ Поддержка health checks, tags filtering
6. ✅ **Симулировать реальное поведение**: Prometheus опрашивает Consul каждые N секунд

**Реализовано:**
- Создан класс `ConsulSD` с поддержкой фильтрации по services и tags
- Автоматическое обнаружение компонентов на canvas, которые могут экспортировать метрики
- Поддержка metadata и tags из конфигурации компонентов
- Кэширование targets с периодическим обновлением (30 секунд)
- Преобразование Consul мета-меток в Prometheus labels

#### 1.3. Static и File Service Discovery ✅ ВЫПОЛНЕНО
**Файлы для изменения:**
- ✅ `src/core/serviceDiscovery/FileSD.ts` - создан новый модуль File SD
- ✅ `src/core/PrometheusEmulationEngine.ts` - интегрирована логика File SD

**Задачи:**
1. ✅ Улучшить существующую `static_configs` поддержку (уже реализована в PrometheusEmulationEngine)
2. ✅ Реализовать `file_sd_configs` для динамического обновления из файлов
3. ✅ Симулировать file watcher для обновления targets (периодическое обновление каждые 5 минут)

**Реализовано:**
- Создан класс `FileSD` с поддержкой виртуальных файлов
- Поддержка формата Prometheus file_sd_configs (JSON массив с targets и labels)
- Периодическое обновление targets (refresh_interval, по умолчанию 5 минут)
- Симуляция чтения файлов через virtual_files в конфиге
- Автоматическое обнаружение компонентов на canvas (fallback если нет virtual_files)
- Использование `ServiceDiscovery` для разрешения портов и хостов

#### 1.4. DNS Service Discovery ✅ ВЫПОЛНЕНО
**Файлы для изменения:**
- ✅ `src/core/serviceDiscovery/DNSSD.ts` - создан новый модуль DNS SD
- ✅ `src/core/PrometheusEmulationEngine.ts` - интегрирована логика DNS SD

**Задачи:**
1. ✅ Реализовать `dns_sd_configs`
2. ✅ Симулировать DNS SRV записи для обнаружения сервисов
3. ✅ Поддержка различных типов DNS записей (A, AAAA, SRV)

**Реализовано:**
- Создан класс `DNSSD` с поддержкой A, AAAA и SRV записей
- Симуляция DNS resolution через поиск компонентов на canvas
- Поддержка SRV формата: `_service._protocol.name`
- Периодическое обновление targets (refresh_interval, по умолчанию 30 секунд)
- Автоматическое сопоставление DNS имен с компонентами по hostname/label
- Использование `ServiceDiscovery` для разрешения портов и хостов

---

### Фаза 2: Улучшение Scraping Engine (ВЫСОКИЙ ПРИОРИТЕТ)

**Цель**: Сделать scraping более реалистичным, как в реальном Prometheus.

**⚠️ Важно понимать:**
- Scraping - это **HTTP GET запросы** от Prometheus к компонентам
- Prometheus сам инициирует запросы по расписанию (scrape_interval)
- Компоненты пассивно отвечают на запросы, экспортируя метрики

#### 2.1. Улучшение поиска targets и scraping логики
**Файлы для изменения:**
- `src/core/PrometheusEmulationEngine.ts` - метод `findNodeByEndpoint` и `scrapeTarget`

**Задачи:**
1. **Симулировать HTTP GET запросы** к `/metrics` endpoints компонентов
2. Реализовать более умный поиск компонентов по connections или Service Discovery
3. Учитывать виртуальные сети, DNS resolution (симуляция)
4. Поддержка множественных endpoints для одного компонента
5. Кэширование результатов поиска для производительности
6. **Важно**: Scraping должен быть **pull-based** - Prometheus инициирует запросы, компоненты отвечают

#### 2.2. HTTP/HTTPS и Authentication
**Задачи:**
1. Поддержка HTTP и HTTPS схем в scrape configs
2. Basic Authentication (username/password)
3. Bearer Token authentication
4. TLS client certificates
5. Симуляция SSL handshake latency

#### 2.3. Relabeling
**Файлы для изменения:**
- `src/core/PrometheusEmulationEngine.ts` - добавить relabeling engine
- `src/components/config/observability/PrometheusConfigAdvanced.tsx` - UI для relabel configs

**Задачи:**
1. Реализовать `relabel_configs` для scrape_configs
2. Поддержка всех relabel actions: `replace`, `keep`, `drop`, `hashmod`, `labelmap`, `labeldrop`, `labelkeep`
3. Регулярные выражения для matching labels
4. Применять relabeling перед сохранением метрик

#### 2.4. Retry и Timeout логика
**Задачи:**
1. Реализовать retry при ошибках scraping (с exponential backoff)
2. Правильная обработка `scrape_timeout`
3. Метрики для failed scrapes, retry attempts

#### 2.5. Metrics Path и Params
**Задачи:**
1. Поддержка кастомных `metrics_path` для каждого job
2. Query parameters в scrape URL
3. Поддержка различных форматов экспорта (Prometheus, OpenMetrics)

---

### Фаза 3: Полные метрики Prometheus (ВЫСОКИЙ ПРИОРИТЕТ)

**Цель**: Добавить все стандартные метрики Prometheus, как в реальной системе.

#### 3.1. TSDB метрики
**Файлы для изменения:**
- `src/core/PrometheusEmulationEngine.ts` - добавить TSDB симуляцию
- `src/core/PrometheusMetricsExporter.ts` - экспорт TSDB метрик

**Задачи:**
1. `prometheus_tsdb_head_samples` - количество samples в head
2. `prometheus_tsdb_head_series` - количество series
3. `prometheus_tsdb_compactions_total` - количество compactions
4. `prometheus_tsdb_wal_corruptions_total` - ошибки WAL
5. `prometheus_tsdb_storage_blocks_bytes` - размер storage blocks
6. Симулировать рост данных со временем
7. Симулировать compaction операции

#### 3.2. Config метрики
**Задачи:**
1. `prometheus_config_last_reload_success_timestamp` - время последней успешной перезагрузки
2. `prometheus_config_last_reload_successful` - статус последней перезагрузки
3. Обновлять при изменении конфига через UI

#### 3.3. Target метрики
**Задачи:**
1. `prometheus_target_interval_length_seconds` - реальный интервал между scrapes
2. `prometheus_target_scrape_pool_sync_total` - количество синхронизаций scrape pool
3. `prometheus_target_scrapes_exceeded_sample_limit_total` - превышение лимита samples
4. Метрики для каждого target: `up{job="...", instance="..."}`, `scrape_duration_seconds`, `scrape_samples_scraped`

#### 3.4. Scraping метрики
**Задачи:**
1. Улучшить существующие `scrape_requests_total`, `scrape_errors_total`
2. Добавить `prometheus_sd_kubernetes_events_total` для Kubernetes SD
3. Добавить `prometheus_sd_consul_rpc_duration_seconds` для Consul SD

---

### Фаза 4: Alerting Engine (СРЕДНИЙ ПРИОРИТЕТ)

**Цель**: Реализовать полноценную evaluation engine для alerting rules.

#### 4.1. PromQL Parser и Evaluator
**Файлы для создания:**
- `src/core/PromQLParser.ts` - парсер PromQL выражений
- `src/core/PromQLEvaluator.ts` - evaluator для выполнения запросов

**Задачи:**
1. Парсинг PromQL выражений (базовый набор: агрегации, функции, операторы)
2. Выполнение запросов над scraped метриками
3. Поддержка range queries для `for` duration в alerting rules
4. Кэширование результатов для производительности

#### 4.2. Alert Evaluation
**Файлы для изменения:**
- `src/core/PrometheusEmulationEngine.ts` - добавить alert evaluation
- `src/components/config/observability/PrometheusConfigAdvanced.tsx` - UI для просмотра активных алертов

**Задачи:**
1. Периодическая evaluation alerting rules (согласно `evaluation_interval`)
2. Отслеживание состояния алертов (pending -> firing)
3. Поддержка `for` duration (alert должен быть true N времени)
4. Генерация alert events при переходе в firing state
5. Метрики: `prometheus_alerting_rules_last_evaluation_timestamp`, `prometheus_alerting_rules_last_evaluation_duration_seconds`

#### 4.3. Alertmanager Integration
**Файлы для изменения:**
- `src/core/PrometheusEmulationEngine.ts` - отправка алертов
- `src/services/connection/rules/alertmanagerRules.ts` - правила подключения к Alertmanager

**Задачи:**
1. Симуляция отправки алертов в Alertmanager (если компонент есть на canvas)
2. Поддержка различных receiver types (webhook, email, slack, etc.)
3. Метрики: `prometheus_notifications_total`, `prometheus_notifications_failed_total`
4. Retry логика при ошибках отправки

#### 4.4. Recording Rules Evaluation
**Задачи:**
1. Evaluation recording rules
2. Сохранение результатов как новые метрики
3. Использование записанных метрик в alerting rules

---

### Фаза 5: Remote Write (НИЗКИЙ ПРИОРИТЕТ)

**Цель**: Реализовать отправку метрик в remote storage.

#### 5.1. Remote Write Protocol
**Файлы для создания:**
- `src/core/PrometheusRemoteWrite.ts` - реализация remote write

**Задачи:**
1. Реализовать Prometheus Remote Write protocol (protobuf format)
2. Batch отправка метрик
3. Поддержка различных backends (Thanos, Cortex, M3DB)
4. Retry и backoff при ошибках
5. Метрики: `prometheus_remote_write_samples_total`, `prometheus_remote_write_samples_failed_total`

#### 5.2. Queue Management
**Задачи:**
1. Очередь для remote write (если backend недоступен)
2. Ограничение размера очереди
3. Dropping старых samples при переполнении

---

### Фаза 6: Storage Simulation (НИЗКИЙ ПРИОРИТЕТ)

**Цель**: Более реалистичная симуляция TSDB.

#### 6.1. Time-Series Simulation
**Задачи:**
1. Симулировать хранение time-series данных
2. Retention policy (удаление старых данных)
3. Compression симуляция
4. Метрики использования диска

#### 6.2. WAL и Compaction
**Задачи:**
1. Симуляция Write-Ahead Log (WAL)
2. Периодические compaction операции
3. Метрики для WAL и compaction

---

### Фаза 7: UI/UX Improvements (СРЕДНИЙ ПРИОРИТЕТ)

**Цель**: Улучшить пользовательский опыт работы с Prometheus.

#### 7.1. Targets Status View
**Файлы для создания:**
- `src/components/config/observability/PrometheusTargetsView.tsx` - компонент для просмотра targets

**Задачи:**
1. Таблица со всеми targets и их статусами (up/down)
2. Показ последнего scrape time, duration, errors
3. Фильтрация и поиск по targets
4. Обновление в реальном времени при эмуляции

#### 7.2. Prometheus UI Preview
**Задачи:**
1. Встроенный preview Prometheus UI (Graph, Alerts, Targets, Status)
2. Выполнение PromQL запросов
3. Визуализация метрик (графики)

#### 7.3. Alerting Rules Validation
**Задачи:**
1. Валидация PromQL выражений при вводе
2. Подсветка синтаксических ошибок
3. Автодополнение для метрик и функций

#### 7.4. Configuration Validation
**Задачи:**
1. Валидация scrape_configs перед сохранением
2. Проверка доступности targets
3. Предупреждения о потенциальных проблемах

---

## Технические детали реализации

### Архитектурные принципы:

1. **Pull-based модель - основа архитектуры**
   - Prometheus **сам инициирует** все подключения (scraping, SD queries)
   - Компоненты **пассивно** экспортируют метрики на `/metrics` endpoint
   - Service Discovery - Prometheus опрашивает источники (Kubernetes API, Consul API)
   - Scraping - Prometheus делает HTTP GET к компонентам по расписанию
   - **НЕ симулировать** push-модель или двусторонние подключения

2. **Реалистичность превыше всего**
   - Все параметры должны соответствовать реальному Prometheus
   - Использовать реальные форматы конфигов (YAML структура)
   - Симулировать реальное поведение (latency, errors, etc.)
   - Service Discovery должен работать **автономно** - Prometheus сам находит targets

3. **Избегать хардкода**
   - Все дефолтные значения должны быть настраиваемыми
   - Использовать ServiceDiscovery для определения портов/hosts
   - Динамическое определение endpoints через connections или SD
   - Connection rules - это **конфигурация**, а не реальное подключение

4. **Расширяемость**
   - Каждый механизм (SD, scraping, alerting) должен быть модульным
   - Легко добавлять новые типы service discovery
   - Поддержка плагинов для custom exporters
   - Каждый компонент должен уметь экспортировать метрики независимо

5. **Производительность**
   - Кэширование результатов поиска targets
   - Оптимизация evaluation alerting rules
   - Batch обработка метрик
   - Периодическое обновление SD targets (не на каждом цикле)

### Структура файлов:

```
src/
├── core/
│   ├── PrometheusEmulationEngine.ts (расширить)
│   ├── PrometheusMetricsExporter.ts (расширить)
│   ├── PromQLParser.ts (новый)
│   ├── PromQLEvaluator.ts (новый)
│   ├── PrometheusRemoteWrite.ts (новый)
│   └── serviceDiscovery/
│       ├── KubernetesSD.ts (новый)
│       ├── ConsulSD.ts (новый)
│       ├── DNSSD.ts (новый)
│       └── FileSD.ts (новый)
├── components/
│   └── config/
│       └── observability/
│           ├── PrometheusConfigAdvanced.tsx (расширить)
│           ├── PrometheusTargetsView.tsx (новый)
│           └── PrometheusUIPreview.tsx (новый)
└── services/
    └── connection/
        └── rules/
            └── alertmanagerRules.ts (новый)
```

### Тестирование:

1. **Unit тесты** для каждого модуля
2. **Integration тесты** для проверки взаимодействия компонентов
3. **E2E тесты** для проверки полного flow (scraping -> storage -> alerting)

---

## Приоритизация задач

### Критичные (делать в первую очередь):
1. ✅ Улучшение Service Discovery (Kubernetes, Consul)
2. ✅ Улучшение scraping engine (relabeling, auth, retry)
3. ✅ Полные метрики Prometheus (TSDB, targets, config)

### Важные (делать во вторую очередь):
4. ✅ Alerting Engine (PromQL parser, evaluation)
5. ✅ UI Improvements (targets view, validation)

### Желательные (делать при наличии времени):
6. ✅ Remote Write
7. ✅ Storage Simulation

---

## Критерии готовности

Компонент считается полностью реалистичным когда:

1. ✅ Поддерживает все основные типы Service Discovery (Kubernetes, Consul, DNS, File, Static)
2. ✅ Scraping работает с authentication, relabeling, retry
3. ✅ Все стандартные метрики Prometheus экспортируются
4. ✅ Alerting rules evaluation работает корректно
5. ✅ UI позволяет удобно настраивать и мониторить Prometheus
6. ✅ Нет хардкода, все параметры настраиваемые
7. ✅ Соответствует реальному поведению Prometheus

---

## Заметки для разработчика

- **НЕ копировать логику из других компонентов** - Prometheus уникален по своей архитектуре (pull-based)
- **Изучать реальный Prometheus** - читать документацию, смотреть исходный код
- **Тестировать на реальных конфигах** - использовать примеры из Prometheus документации
- **Следовать правилам курсора** - безопасный доступ к данным, валидация, error handling
- **Помнить про pull-модель**:
  - Prometheus сам инициирует все запросы (scraping, SD queries)
  - Компоненты пассивно отвечают на запросы
  - Service Discovery - Prometheus опрашивает источники периодически
  - Connection rules в UI - это конфигурация, а не реальное подключение
- **Service Discovery критичен** - без него Prometheus не может найти targets автоматически

---

## Следующие шаги

1. **Начать с Фазы 1 (Service Discovery)** - это критично для реалистичности
   - Без SD Prometheus не может автоматически находить targets
   - Это основа pull-модели - Prometheus сам опрашивает источники
   - Connection rules - это просто удобный способ добавить static config

2. **Параллельно можно работать над Фазой 2 (Scraping Engine)**
   - Убедиться что scraping симулирует HTTP GET запросы от Prometheus
   - Компоненты должны отвечать на запросы, а не инициировать их

3. **После базовых механизмов - добавить метрики (Фаза 3)**
   - Убедиться что компоненты экспортируют метрики независимо от Prometheus

4. **Затем - Alerting (Фаза 4)**
   - Alerting также pull-based - Prometheus сам оценивает правила

5. **UI improvements можно делать параллельно с основными фазами**
   - Но помнить: connection rules - это конфигурация, а не реальное подключение

---

## Статус реализации

**Дата создания плана**: 2026-01-25  
**Версия**: 1.1  
**Последнее обновление**: 2026-01-25

### ✅ Выполнено (Фаза 1 - полностью):

1. **Kubernetes Service Discovery** ✅
   - Создан `KubernetesSD.ts` с поддержкой всех основных roles
   - Интеграция с `KubernetesEmulationEngine`
   - Автоматическое обнаружение Pods с annotations
   - Динамическое обновление targets
   - Кэширование с периодическим обновлением

2. **Consul Service Discovery** ✅
   - Создан `ConsulSD.ts` с поддержкой фильтрации
   - Автоматическое обнаружение компонентов на canvas
   - Поддержка metadata и tags
   - Кэширование с периодическим обновлением

3. **File Service Discovery** ✅
   - Создан `FileSD.ts` с поддержкой виртуальных файлов
   - Периодическое обновление targets (refresh_interval)
   - Симуляция file watcher через периодическое чтение
   - Интеграция в `PrometheusEmulationEngine`

4. **DNS Service Discovery** ✅
   - Создан `DNSSD.ts` с поддержкой A, AAAA, SRV записей
   - Симуляция DNS resolution через поиск компонентов
   - Периодическое обновление targets (refresh_interval)
   - Интеграция в `PrometheusEmulationEngine`

5. **Интеграция всех SD в PrometheusEmulationEngine** ✅
   - Поддержка `kubernetes_sd_configs`, `consul_sd_configs`, `file_sd_configs`, `dns_sd_configs`
   - Автоматическое обновление targets из всех SD механизмов
   - Интеграция с `EmulationEngine`
   - Периодическое обновление targets (каждые 30 секунд или согласно refresh_interval)

### ✅ Выполнено (Фаза 2 - полностью, Фаза 3 - полностью):

3. **Улучшение Scraping Engine** (Фаза 2) ✅
   - ✅ Relabeling Engine реализован (`PrometheusRelabeling.ts`)
   - ✅ Поддержка всех relabel actions: `replace`, `keep`, `drop`, `hashmod`, `labelmap`, `labeldrop`, `labelkeep`
   - ✅ HTTP/HTTPS схемы в scrape configs
   - ✅ Basic Authentication (username/password)
   - ✅ Bearer Token authentication
   - ✅ TLS config поддержка
   - ✅ Retry логика с exponential backoff (max 3 retries)
   - ✅ Симуляция SSL handshake latency для HTTPS

4. **Полные метрики Prometheus** (Фаза 3) ✅
   - ✅ TSDB метрики: `prometheus_tsdb_head_samples`, `prometheus_tsdb_head_series`, `prometheus_tsdb_compactions_total`, `prometheus_tsdb_wal_corruptions_total`, `prometheus_tsdb_storage_blocks_bytes`
   - ✅ Config метрики: `prometheus_config_last_reload_success_timestamp`, `prometheus_config_last_reload_successful`
   - ✅ Target метрики: `up{job,instance}`, `scrape_duration_seconds{job,instance}`, `scrape_samples_scraped{job,instance}`
   - ✅ Метод `exportPrometheusMetrics()` для экспорта всех метрик Prometheus в Prometheus format

### ✅ Выполнено (Фаза 4 - полностью):

6. **Alerting Engine** (Фаза 4) ✅
   - ✅ PromQL Parser и Evaluator - реализовано (`PromQLParser.ts`, `PromQLEvaluator.ts`)
   - ✅ Alert Evaluation - реализовано (периодическая evaluation, pending->firing, for duration)
   - ✅ Alertmanager Integration - реализовано (симуляция отправки алертов)
   - ✅ Recording Rules Evaluation - реализовано
   - ✅ Метрики alerting: `prometheus_alerting_rules_last_evaluation_timestamp`, `prometheus_alerting_rules_last_evaluation_duration_seconds`, `prometheus_notifications_total`, `prometheus_notifications_failed_total`

**Реализовано**:
- Создан `PromQLParser.ts` с поддержкой базовых PromQL операций (метрики, операторы, сравнения, функции, агрегации)
- Создан `PromQLEvaluator.ts` для выполнения PromQL запросов над scraped метриками
- Интеграция в `PrometheusEmulationEngine.ts`:
  - Периодическая evaluation alerting rules согласно `evaluation_interval`
  - Отслеживание состояния алертов (inactive -> pending -> firing)
  - Поддержка `for` duration (alert должен быть true N времени перед firing)
  - Генерация alert events при переходе в firing state
  - Симуляция отправки алертов в Alertmanager
  - Evaluation recording rules и сохранение результатов
- Методы `getAlertStates()` и `getRecordingRuleResults()` для доступа к результатам
- Метрики alerting экспортируются в `exportPrometheusMetrics()`

**Реалистичность**:
- Pull-based модель соблюдена - Prometheus сам оценивает правила периодически
- Соответствует реальному поведению Prometheus (pending -> firing с учетом for duration)
- Симуляция отправки алертов в Alertmanager (HTTP POST в реальности)
- Отсутствие хардкода - все параметры настраиваемые через конфигурацию

### ✅ Выполнено (Фаза 7 - частично):

5. **UI улучшения** (Фаза 7)
   - ✅ Targets Status View - реализовано (`PrometheusTargetsView.tsx`)
     - Таблица со всеми targets и их статусами (up/down)
     - Показ последнего scrape time, duration, errors, samples scraped, retries
     - Фильтрация по статусу (all/up/down) и job
     - Поиск по endpoint, job, labels
     - Обновление в реальном времени при эмуляции (каждую секунду)
     - Статистика: Total Targets, Up, Down
     - Интегрировано в `PrometheusConfigAdvanced.tsx` как первая вкладка "Targets"
   - ⚠️ Улучшить UI для настройки Kubernetes SD и Consul SD (частично - есть структура, но нужно улучшить)
   - ⚠️ Prometheus UI Preview - не реализовано
   - ⚠️ Alerting Rules Validation - не реализовано

7. **Remote Write** (Фаза 5)
   - ⚠️ Remote Write Protocol - не реализовано
   - ⚠️ Queue Management - не реализовано

8. **Storage Simulation** (Фаза 6)
   - ⚠️ Time-Series Simulation - не реализовано
   - ⚠️ WAL и Compaction - не реализовано (только базовая симуляция)

### 📝 Заметки:

- Все реализовано без хардкода - используется `ServiceDiscovery` для разрешения имен и портов
- Pull-based модель соблюдена - Prometheus сам инициирует запросы к SD источникам
- Периодичность обновления SD targets: 30 секунд (как в реальном Prometheus)
- Интеграция с `KubernetesEmulationEngine` позволяет использовать реальные Kubernetes ресурсы
