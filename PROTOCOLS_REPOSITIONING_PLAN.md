# План перепозиционирования Protocols & APIs из отдельных сервисов в технологические коннекторы

## 🎯 Цель и концепция

### Проблема текущей реализации
Сейчас REST, GraphQL, SOAP, WebSocket, Webhook, gRPC реализованы как **отдельные узлы на канвасе** (как будто это отдельные сервисы). Это **неправильно**, потому что:

1. **В реальности протоколы - это не сервисы**, а способы общения между сервисами
2. **Протоколы не имеют собственной бизнес-логики** - они просто определяют формат и способ передачи данных
3. **Архитектура становится запутанной**: вместо простой связи `CRM → Payment Gateway` получается `CRM → REST API → Payment Gateway`, где REST API - лишний узел

### Правильная концепция
**Протоколы должны быть атрибутами соединений**, а не отдельными узлами:
- Соединение `CRM → Payment Gateway` может использовать протокол REST
- Соединение `Mobile App → BFF` может использовать протокол GraphQL
- Соединение `Notification Service → Client` может использовать WebSocket

### Визуальное сравнение

**ДО (неправильно):**
```
┌──────┐      ┌─────────┐      ┌──────────────┐
│ CRM  │─────▶│ REST API │─────▶│ Payment      │
│      │      │ (узел!)  │      │ Gateway      │
└──────┘      └─────────┘      └──────────────┘
```

**ПОСЛЕ (правильно):**
```
┌──────┐  ┌──────────────┐
│ CRM  │──│ REST (протокол│──▶│ Payment      │
│      │  │ на соединении)│   │ Gateway      │
└──────┘  └──────────────┘   └──────────────┘
```

### Почему это важно
1. **Реалистичность**: Соответствует реальной архитектуре, где протоколы - это не отдельные сервисы
2. **Простота**: Меньше узлов на канвасе = проще понимать архитектуру
3. **Гибкость**: Один сервис может общаться с разными сервисами через разные протоколы
4. **Метрики**: Метрики протоколов (latency, error rate) должны быть метриками соединений, а не отдельных узлов

---

## 📊 Текущая ситуация (детальный анализ)

### Что есть сейчас:

#### 1. COMPONENT_LIBRARY (`src/data/components.ts`)
**Текущий код:**
```typescript
// APIs & Protocols
{ id: 'rest', type: 'rest', label: 'REST API', icon: '🔌', color: '#61DAFB', category: 'api' },
{ id: 'grpc', type: 'grpc', label: 'gRPC', icon: '⚡', color: '#244C5A', category: 'api' },
{ id: 'graphql', type: 'graphql', label: 'GraphQL', icon: '🔮', color: '#DE33A6', category: 'api' },
{ id: 'soap', type: 'soap', label: 'SOAP', icon: '🧼', color: '#2196F3', category: 'api' },
{ id: 'websocket', type: 'websocket', label: 'WebSocket', icon: '🔄', color: '#010101', category: 'api' },
{ id: 'webhook', type: 'webhook', label: 'Webhook Endpoint', icon: '📬', color: '#0FA3B1', category: 'api' },
```

**Проблема**: Протоколы отображаются в библиотеке компонентов как отдельные узлы, которые можно добавить на канвас.

---

#### 2. DataFlowEngine (`src/core/DataFlowEngine.ts`)
**Текущий код (строки 109-114):**
```typescript
// APIs - transform and route data
this.registerHandler('rest', this.createAPIHandler('rest'));
this.registerHandler('grpc', this.createAPIHandler('grpc'));
this.registerHandler('graphql', this.createAPIHandler('graphql'));
this.registerHandler('websocket', this.createAPIHandler('websocket'));
this.registerHandler('webhook', this.createWebhookHandler());
```

**Текущий код обработки REST (строки 2849-2921):**
```typescript
if (type === 'rest') {
  return {
    processData: (node, message, config) => {
      const routingEngine = emulationEngine.getRestApiRoutingEngine(node.id);
      // ... обработка как отдельного узла
    },
    transformData: (node, message, targetType, config) => {
      // REST API transforms to JSON
      if (message.format !== 'json') {
        message.format = 'json';
      }
      return message;
    },
    getSupportedFormats: () => ['json', 'xml'],
  };
}
```

**Проблема**: Протоколы обрабатываются как отдельные узлы с собственной логикой обработки данных.

---

#### 3. EmulationEngine (`src/core/EmulationEngine.ts`)
**Текущий код симуляции (строки 2000-2011):**
```typescript
case 'rest':
case 'grpc':
case 'graphql':
case 'websocket':
  this.simulateAPI(node, config, metrics, hasIncomingConnections);
  break;
case 'webhook':
  this.simulateWebhook(node, config, metrics, hasIncomingConnections);
  break;
case 'soap':
  this.simulateSOAP(node, config, metrics, hasIncomingConnections);
  break;
```

**Текущий код инициализации (строки 617-969):**
```typescript
if (node.type === 'rest') {
  this.initializeRestApiRoutingEngine(node);
}
if (node.type === 'grpc') {
  this.initializeGRPCRoutingEngine(node);
}
if (node.type === 'graphql') {
  this.initializeGraphQLEngine(node);
}
// ... и т.д.
```

**Проблема**: Для каждого протокола создаются отдельные routing engines и симуляции, как будто это отдельные сервисы.

---

#### 4. Connection Rules (`src/services/connection/rules/`)
**Текущий код `bffRules.ts` (строки 12-36):**
```typescript
targetTypes: ['rest', 'grpc', 'graphql', 'websocket', 'soap', 'webhook'],

updateSourceConfig: (bff, target, connection, metadata) => {
  // ...
  let protocol: 'http' | 'grpc' | 'graphql' = 'http';
  if (target.type === 'grpc') {
    protocol = 'grpc';
  } else if (target.type === 'graphql') {
    protocol = 'graphql';
  }
  // ...
}
```

**Проблема**: Протоколы используются как `targetTypes`, т.е. BFF может подключаться к узлу "REST API", а не к бизнес-сервису через протокол REST.

---

#### 5. UI Configs (`src/components/config/api/`)
**Текущие файлы:**
- `src/components/config/api/ApiConfig.tsx` - конфиг для REST API узла
- `src/components/config/api/GraphQLConfigAdvanced.tsx` - конфиг для GraphQL узла
- `src/components/config/api/WebhookConfigAdvanced.tsx` - конфиг для Webhook узла
- `src/components/config/api/ApiConfigExtended.tsx` - расширенный конфиг для API узлов

**Проблема**: У каждого протокола есть свой UI конфиг, как будто это отдельный компонент, который нужно настраивать.

---

#### 6. CanvasConnection (`src/types/index.ts`)
**Текущий код (строки 74-84):**
```typescript
export interface CanvasConnection {
  id: string;
  source: string;
  target: string;
  type: 'sync' | 'async' | 'http' | 'grpc' | 'websocket';  // ❌ Не все протоколы
  label?: string;
  data?: ConnectionConfig;
  // ...
}
```

**Текущий ConnectionConfig (строки 86-103):**
```typescript
export interface ConnectionConfig {
  latencyMs?: number;
  bandwidthMbps?: number;
  packetLossPercent?: number;
  jitterMs?: number;
  priorityLevel?: 'low' | 'medium' | 'high' | 'critical';
  retryCount?: number;
  timeoutMs?: number;
  enableMonitoring?: boolean;
  [key: string]: unknown;  // ❌ Нет явной поддержки протоколов
}
```

**Проблема**: 
- `type` поддерживает только `'http' | 'grpc' | 'websocket'`, но не `'rest' | 'graphql' | 'soap' | 'webhook'`
- Нет явной поддержки протокол-специфичных настроек

---

#### 7. ConnectionMetadata (`src/services/connection/types.ts`)
**Текущий код (строки 6-14):**
```typescript
export interface ConnectionMetadata {
  protocol?: 'http' | 'grpc' | 'tcp' | 'udp' | 'websocket' | 'kafka' | 'rabbitmq' | 'async' | 'sync';
  // ❌ Нет 'rest' | 'graphql' | 'soap' | 'webhook'
  sourcePort?: number;
  targetPort?: number;
  sourceHost?: string;
  targetHost?: string;
  path?: string;
  endpoint?: string;
}
```

**Проблема**: `protocol` не включает все нужные протоколы.

---

#### 8. ServiceDiscovery (`src/services/connection/ServiceDiscovery.ts`)
**Текущий код определения протокола (строки 146-160):**
```typescript
let protocol: ConnectionMetadata['protocol'] = 'http';
if (connection.type === 'grpc') {
  protocol = 'grpc';
} else if (connection.type === 'websocket') {
  protocol = 'websocket';
} else if (target.type === 'kafka') {
  protocol = 'kafka';
} else if (target.type === 'rabbitmq' || target.type === 'activemq') {
  protocol = 'rabbitmq';
}
// ❌ Нет обработки 'rest' | 'graphql' | 'soap' | 'webhook'
```

**Проблема**: Протокол определяется из типа целевого узла, а не из атрибутов соединения.

---

## 🎯 Целевое состояние (детальное описание)

### Как должно быть:

#### 1. Протоколы - это атрибуты соединений
**Пример использования:**
```typescript
// Соединение CRM → Payment Gateway через REST
const connection: CanvasConnection = {
  id: 'conn-1',
  source: 'crm-node-id',
  target: 'payment-gateway-node-id',
  type: 'rest',  // ✅ Протокол на соединении
  data: {
    protocol: 'rest',
    protocolConfig: {
      httpMethod: 'POST',
      contentType: 'json',
    },
    latencyMs: 50,
    // ... другие сетевые параметры
  }
};
```

#### 2. Протокол влияет на обработку данных
**Пример трансформации в DataFlowEngine:**
```typescript
// При прохождении сообщения через соединение с протоколом REST
if (connection.type === 'rest' || connection.data?.protocol === 'rest') {
  // Преобразовать в JSON формат
  message.format = 'json';
  message.payload = JSON.stringify(message.payload);
  // Добавить HTTP заголовки
  message.metadata = {
    ...message.metadata,
    'Content-Type': 'application/json',
    'Accept': 'application/json',
  };
}
```

#### 3. Протокол влияет на метрики соединения
**Пример расчета метрик:**
```typescript
// В EmulationEngine при расчете метрик соединения
const protocol = connection.type || connection.data?.protocol || 'http';
const baseLatency = connection.data?.latencyMs || 50;

// Протокол-специфичные модификаторы
const protocolLatencyMultipliers = {
  'rest': 1.0,      // Базовый
  'graphql': 1.1,   // Немного медленнее из-за парсинга запросов
  'soap': 1.3,      // Медленнее из-за XML парсинга
  'grpc': 0.7,      // Быстрее из-за бинарного протокола
  'websocket': 0.9, // Быстрее для real-time
  'webhook': 1.0,   // Базовый
};

const finalLatency = baseLatency * protocolLatencyMultipliers[protocol];
```

#### 4. Примеры архитектуры

**Сценарий 1: Мобильное приложение → BFF → Backend Services**
```
┌─────────────┐
│ Mobile App   │
└──────┬───────┘
       │ GraphQL (протокол на соединении)
       ▼
┌─────────────┐
│ BFF Service │
└──┬──────┬───┘
   │ REST │ gRPC (протоколы на соединениях)
   ▼      ▼
┌─────┐ ┌─────┐
│User │ │Order│
│Svc  │ │Svc  │
└─────┘ └─────┘
```

**Сценарий 2: Kong Gateway → Backend Services**
```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │ HTTP
       ▼
┌─────────────┐
│Kong Gateway │
└──┬──────┬───┘
   │ REST │ GraphQL (протоколы на соединениях)
   ▼      ▼
┌─────┐ ┌─────┐
│API  │ │Graph│
│Svc  │ │QL   │
└─────┘ └─────┘
```

#### 5. UI для настройки протоколов
**В правом сайдбаре при выборе соединения:**
```
┌─────────────────────────┐
│ Connection Settings      │
├─────────────────────────┤
│ Network Parameters       │
│ - Latency: 50ms         │
│ - Bandwidth: 100 Mbps   │
│                         │
│ Protocol                 │
│ [REST ▼]                 │
│                         │
│ REST Settings            │
│ - HTTP Method: [POST ▼] │
│ - Content Type: [JSON ▼]│
└─────────────────────────┘
```

#### 6. Визуализация протоколов на канвасе
- **Цвет линии соединения** зависит от протокола:
  - REST → синий
  - GraphQL → фиолетовый
  - gRPC → зеленый
  - SOAP → оранжевый
  - WebSocket → желтый
  - Webhook → красный
- **Иконка протокола** на линии соединения
- **Tooltip** при наведении показывает протокол и его настройки

---

## Этап 1: Расширение типов для поддержки протоколов в соединениях ✅ ВЫПОЛНЕНО

### 🎯 Цель этапа
Расширить типы TypeScript, чтобы протоколы могли быть атрибутами соединений, а не только типами узлов.

### ✅ Статус: ЗАВЕРШЕНО
- Расширен `CanvasConnection.type` - добавлены все протоколы
- Расширен `ConnectionMetadata.protocol` - добавлены все протоколы
- Добавлены `protocol` и `protocolConfig` в `ConnectionConfig`

### 📝 Задачи:

#### 1. Расширить `CanvasConnection.type` (`src/types/index.ts`, строка 78)

**Текущий код:**
```typescript
type: 'sync' | 'async' | 'http' | 'grpc' | 'websocket';
```

**Новый код:**
```typescript
type: 'sync' | 'async' | 'rest' | 'graphql' | 'soap' | 'grpc' | 'websocket' | 'webhook' | 'http';
```

**Объяснение**: Добавляем все протоколы в тип соединения. `'http'` оставляем для обратной совместимости (можно использовать как синоним `'rest'`).

---

#### 2. Расширить `ConnectionMetadata.protocol` (`src/services/connection/types.ts`, строка 7)

**Текущий код:**
```typescript
protocol?: 'http' | 'grpc' | 'tcp' | 'udp' | 'websocket' | 'kafka' | 'rabbitmq' | 'async' | 'sync';
```

**Новый код:**
```typescript
protocol?: 'rest' | 'graphql' | 'soap' | 'grpc' | 'websocket' | 'webhook' | 'http' | 'tcp' | 'udp' | 'kafka' | 'rabbitmq' | 'async' | 'sync';
```

**Объяснение**: Добавляем все протоколы в метаданные соединения. Это используется в connection rules для определения протокола.

---

#### 3. Добавить в `ConnectionConfig` (`src/types/index.ts`, после строки 100)

**Новый код:**
```typescript
export interface ConnectionConfig {
  // Network parameters (существующие)
  latencyMs?: number;
  bandwidthMbps?: number;
  packetLossPercent?: number;
  jitterMs?: number;
  
  // Traffic characteristics (существующие)
  priorityLevel?: 'low' | 'medium' | 'high' | 'critical';
  retryCount?: number;
  timeoutMs?: number;
  
  // Monitoring (существующие)
  enableMonitoring?: boolean;
  
  // ✨ НОВОЕ: Protocol-specific settings
  protocol?: 'rest' | 'graphql' | 'soap' | 'grpc' | 'websocket' | 'webhook';
  protocolConfig?: {
    // REST-specific
    httpMethod?: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH';
    contentType?: 'json' | 'xml' | 'form-data';
    headers?: Record<string, string>;
    
    // GraphQL-specific
    query?: string;
    operationName?: string;
    variables?: Record<string, any>;
    
    // SOAP-specific
    soapAction?: string;
    wsdlUrl?: string;
    namespace?: string;
    
    // gRPC-specific
    serviceName?: string;
    methodName?: string;
    metadata?: Record<string, string>;
    
    // WebSocket-specific
    wsProtocol?: string;
    subprotocols?: string[];
    binaryType?: 'blob' | 'arraybuffer';
    
    // Webhook-specific
    webhookEvent?: string;
    signatureHeader?: string;
    secret?: string;
  };
  
  // Custom (существующее)
  [key: string]: unknown;
}
```

**Объяснение**: 
- `protocol` - явно указывает протокол соединения (может дублировать `connection.type`, но более явно)
- `protocolConfig` - протокол-специфичные настройки, которые будут использоваться при обработке данных

---

### 📁 Файлы для изменения:

1. **`src/types/index.ts`**:
   - Строка 78: расширить `CanvasConnection.type`
   - После строки 100: добавить `protocol` и `protocolConfig` в `ConnectionConfig`

2. **`src/services/connection/types.ts`**:
   - Строка 7: расширить `ConnectionMetadata.protocol`

---

### ✅ Критерии успеха этапа:
- [ ] TypeScript компилируется без ошибок
- [ ] Все типы расширены корректно
- [ ] Обратная совместимость сохранена (старые соединения с `type: 'http'` работают)

---

## Этап 2: Удаление протоколов из COMPONENT_LIBRARY ✅ ВЫПОЛНЕНО

### 🎯 Цель этапа
Убрать протоколы из библиотеки компонентов, чтобы их нельзя было добавить на канвас как отдельные узлы.

### ✅ Статус: ЗАВЕРШЕНО
- Удалены протоколы (rest, grpc, graphql, soap, websocket, webhook) из `COMPONENT_LIBRARY`
- Удалена категория 'api' из `COMPONENT_CATEGORIES`
- Удален тип 'api' из `ComponentCategory`

### 📝 Задачи:

#### 1. Удалить протоколы из `COMPONENT_LIBRARY` (`src/data/components.ts`, строки 68-74)

**Текущий код:**
```typescript
// APIs & Protocols
{ id: 'rest', type: 'rest', label: 'REST API', icon: '🔌', color: '#61DAFB', category: 'api' },
{ id: 'grpc', type: 'grpc', label: 'gRPC', icon: '⚡', color: '#244C5A', category: 'api' },
{ id: 'graphql', type: 'graphql', label: 'GraphQL', icon: '🔮', color: '#DE33A6', category: 'api' },
{ id: 'soap', type: 'soap', label: 'SOAP', icon: '🧼', color: '#2196F3', category: 'api' },
{ id: 'websocket', type: 'websocket', label: 'WebSocket', icon: '🔄', color: '#010101', category: 'api' },
{ id: 'webhook', type: 'webhook', label: 'Webhook Endpoint', icon: '📬', color: '#0FA3B1', category: 'api' },
```

**Новый код:**
```typescript
// Удалить весь блок "APIs & Protocols"
// Протоколы больше не отдельные компоненты
```

---

#### 2. Обновить категорию `'api'` в `COMPONENT_CATEGORIES` (`src/data/components.ts`, строка 97)

**Текущий код:**
```typescript
{ id: 'api', label: 'Protocols & APIs', icon: '🔌' },
```

**Варианты:**
- **Вариант A**: Удалить категорию полностью (если она больше не используется)
- **Вариант B**: Переименовать в что-то другое (если нужна для других целей)

**Рекомендация**: Удалить, так как протоколы больше не компоненты.

**Новый код:**
```typescript
// Удалить строку с category: 'api'
```

---

#### 3. Проверить использование категории `'api'` в коде

**Где может использоваться:**
- Фильтрация компонентов в UI
- Группировка в сайдбаре
- Экспорт/импорт конфигураций

**Действие**: Найти все использования `category === 'api'` и удалить/обновить.

---

### 📁 Файлы для изменения:

1. **`src/data/components.ts`**:
   - Строки 68-74: удалить протоколы из `COMPONENT_LIBRARY`
   - Строка 97: удалить или обновить категорию `'api'` в `COMPONENT_CATEGORIES`

2. **Проверить использование `category: 'api'`**:
   - `src/components/layout/Sidebar.tsx` (если есть фильтрация)
   - `src/store/useComponentLibraryStore.ts` (если есть)
   - Другие места, где используется категория

---

### ⚠️ Важные замечания:

1. **Обратная совместимость**: Существующие канвасы могут иметь узлы протоколов. Их нужно будет мигрировать (Этап 7).

2. **Integration компоненты остаются**: 
   - `kong`, `apigee`, `mulesoft`, `graphql-gateway`, `bff-service`, `webhook-relay` остаются в библиотеке
   - Они в категории `'integration'`, не `'api'`

---

### ✅ Критерии успеха этапа:
- [ ] Протоколы не отображаются в библиотеке компонентов
- [ ] TypeScript компилируется без ошибок
- [ ] UI не ломается (нет ошибок при открытии сайдбара)
- [ ] Integration компоненты остаются доступными

---

## Этап 3: Рефакторинг DataFlowEngine - протоколы как атрибуты соединений ✅ ВЫПОЛНЕНО

### ✅ Статус: ЗАВЕРШЕНО
- Создан `ProtocolTransformer` для обработки протоколов в соединениях
- Удалены handlers для протоколов из `registerDefaultHandlers`
- Интегрирован `ProtocolTransformer` в `transformMessageIfNeeded` и `calculateTransitTime`
- Протоколы теперь обрабатываются на уровне соединений, а не узлов

### Задачи:
1. **Удалить handlers для протоколов**:
   - Убрать `registerHandler('rest', ...)`, `registerHandler('graphql', ...)`, etc.
   - Убрать `createAPIHandler()`, `createWebhookHandler()`

2. **Добавить обработку протоколов в соединениях**:
   - При обработке сообщения через соединение, проверять `connection.type` или `connection.data?.protocol`
   - Применять трансформацию данных в зависимости от протокола:
     - REST → JSON
     - GraphQL → JSON (с query/mutation)
     - SOAP → XML
     - gRPC → Binary
     - WebSocket → Text/Binary
     - Webhook → JSON/XML

3. **Создать `ProtocolTransformer`** (`src/core/ProtocolTransformer.ts`):
   ```typescript
   class ProtocolTransformer {
     transformForProtocol(message: DataMessage, protocol: string, config?: any): DataMessage;
     getSupportedFormats(protocol: string): string[];
     calculateProtocolLatency(protocol: string, baseLatency: number): number;
   }
   ```

4. **Интегрировать в `processConnection()`**:
   - При прохождении сообщения через соединение, применять протокольную трансформацию

### Файлы для изменения:
- `src/core/DataFlowEngine.ts` - удалить handlers протоколов, добавить обработку в соединениях
- `src/core/ProtocolTransformer.ts` - новый файл для трансформации протоколов

---

## Этап 4: Рефакторинг EmulationEngine - убрать симуляцию протоколов как узлов ✅ ВЫПОЛНЕНО

### ✅ Статус: ЗАВЕРШЕНО
- Удалены case'ы для протоколов (rest, grpc, graphql, soap, websocket, webhook) из `updateComponentMetrics`
- Добавлен `ProtocolTransformer` в `EmulationEngine`
- Обновлен `calculateConnectionLatency` для учета протоколов через multipliers
- Методы `simulateAPI`, `simulateSOAP`, `simulateWebhook` оставлены для обратной совместимости (используются другими компонентами)

### Задачи:
1. **Удалить симуляции протоколов**:
   - Убрать `simulateAPI()`, `simulateSOAP()`, `simulateWebhook()`, `simulateGraphQL()`
   - Убрать case'ы для `'rest'`, `'grpc'`, `'graphql'`, `'soap'`, `'websocket'`, `'webhook'` из `updateComponentMetrics()`

2. **Удалить routing engines для протоколов**:
   - Убрать `restApiRoutingEngines`, `grpcRoutingEngines`, `graphQLEngines`, `soapEngines`, `websocketEngines`, `webhookEngines`
   - Убрать методы инициализации: `initializeRestApiRoutingEngine()`, `initializeGRPCRoutingEngine()`, etc.

3. **Добавить метрики протоколов в соединения**:
   - При расчете метрик соединения учитывать протокол:
     - Latency зависит от протокола (gRPC быстрее REST)
     - Error rate зависит от протокола (SOAP выше, gRPC ниже)
     - Throughput зависит от протокола (WebSocket для streaming)

4. **Создать `ConnectionProtocolMetrics`**:
   ```typescript
   interface ConnectionProtocolMetrics {
     protocol: string;
     requestsPerSecond: number;
     latency: number;
     errorRate: number;
     bytesTransferred: number;
   }
   ```

### Файлы для изменения:
- `src/core/EmulationEngine.ts` - удалить симуляции протоколов, добавить метрики соединений
- Удалить файлы routing engines (если они не используются Integration компонентами):
  - `src/core/RestApiRoutingEngine.ts` (если только для REST узлов)
  - `src/core/GRPCRoutingEngine.ts` (если только для gRPC узлов)
  - `src/core/GraphQLEmulationEngine.ts` (если только для GraphQL узлов, не Gateway)
  - `src/core/SOAPEmulationEngine.ts`
  - `src/core/WebSocketEmulationEngine.ts`
  - `src/core/WebhookEmulationEngine.ts`

---

## Этап 5: Рефакторинг Connection Rules - протоколы как атрибуты ✅ ВЫПОЛНЕНО

### ✅ Статус: ЗАВЕРШЕНО
- Обновлены connection rules: убраны протоколы из `targetTypes` (bffRules, kongRules, apiGatewayRules)
- `targetTypes` изменены на `'*'` - компоненты могут подключаться к любым сервисам
- Обновлен `ServiceDiscovery.getConnectionMetadata()` для определения протокола из `connection.type` или `connection.data.protocol`
- Протокол определяется из соединения, а не из типа целевого компонента

### Задачи:
1. **Изменить connection rules**:
   - Убрать протоколы из `targetTypes`
   - Вместо `targetTypes: ['rest', 'grpc', 'graphql']` использовать `targetTypes: '*'` или конкретные бизнес-компоненты
   - При создании соединения автоматически определять протокол на основе конфига или типа компонента

2. **Обновить `ServiceDiscovery.getConnectionMetadata()`**:
   - Определять протокол из:
     - `connection.type` (если установлен)
     - `connection.data?.protocol` (если установлен)
     - Конфига целевого компонента (если есть настройка протокола)
     - Дефолтного протокола для типа компонента

3. **Обновить правила**:
   - `kongRules.ts` - Kong может подключаться к любым сервисам, протокол определяется автоматически
   - `bffRules.ts` - BFF может подключаться к любым сервисам, протокол определяется автоматически
   - `mulesoftRules.ts` - MuleSoft может подключаться к любым сервисам, протокол определяется автоматически
   - `graphqlGatewayRules.ts` - GraphQL Gateway подключается к GraphQL сервисам, но протокол - это атрибут соединения

### Файлы для изменения:
- `src/services/connection/rules/kongRules.ts`
- `src/services/connection/rules/bffRules.ts`
- `src/services/connection/rules/mulesoftRules.ts`
- `src/services/connection/rules/graphqlGatewayRules.ts`
- `src/services/connection/rules/apiGatewayRules.ts`
- `src/services/connection/ServiceDiscovery.ts`

---

## Этап 6: Рефакторинг UI - настройка протоколов на уровне соединений ✅ ВЫПОЛНЕНО

### ✅ Статус: ЗАВЕРШЕНО
- Добавлена секция "Protocol" в `ConnectionPropertiesPanel` для настройки протоколов соединений
- Реализован выбор протокола (REST, GraphQL, SOAP, gRPC, WebSocket, Webhook)
- Добавлены протокол-специфичные настройки для каждого протокола:
  - REST: HTTP Method, Content Type
  - GraphQL: Operation Name
  - SOAP: SOAP Action, WSDL URL
  - gRPC: Service Name, Method Name
  - WebSocket: Subprotocol, Binary Type
  - Webhook: Event Type, Signature Header
- Удалены case'ы для протоколов из `ComponentConfigRenderer` (протоколы больше не узлы)
- Протоколы настраиваются на уровне соединений в правом сайдбаре

### Задачи:
1. **Удалить UI конфиги для протоколов**:
   - Удалить `src/components/config/api/ApiConfig.tsx`
   - Удалить `src/components/config/api/GraphQLConfigAdvanced.tsx`
   - Удалить `src/components/config/api/WebhookConfigAdvanced.tsx`
   - Удалить `src/components/config/api/ApiConfigExtended.tsx`
   - Удалить `src/components/config/api/profiles.ts` (если только для протоколов)

2. **Удалить из `ComponentConfigRenderer`**:
   - Убрать case'ы для `'rest'`, `'grpc'`, `'graphql'`, `'soap'`, `'websocket'`, `'webhook'`

3. **Создать UI для настройки протоколов соединений**:
   - Добавить в правый сайдбар (где настройки сетевых параметров) секцию "Protocol"
   - Позволить выбирать протокол для соединения:
     - REST, GraphQL, SOAP, gRPC, WebSocket, Webhook
   - Показывать протокол-специфичные настройки:
     - REST: HTTP method, content type
     - GraphQL: query template, operation name
     - SOAP: SOAP action, WSDL URL
     - gRPC: service name, method name
     - WebSocket: subprotocols
     - Webhook: event type, signature header

4. **Обновить визуализацию соединений**:
   - Показывать протокол на линии соединения (иконка или цвет)
   - В tooltip соединения показывать протокол и его настройки

### Файлы для изменения:
- Удалить: `src/components/config/api/*.tsx` (кроме тех, что нужны для Integration компонентов)
- `src/components/config/ComponentConfigRenderer.tsx` - убрать case'ы протоколов
- `src/components/canvas/CanvasConnection.tsx` (если есть) - добавить визуализацию протокола
- Создать: `src/components/config/connection/ConnectionProtocolConfig.tsx` - новый компонент для настройки протокола соединения

---

## Этап 7: Миграция существующих данных ✅ ВЫПОЛНЕНО

### ✅ Статус: ЗАВЕРШЕНО
- Создан `MigrationService` (`src/utils/migration.ts`) для миграции протоколов из узлов в соединения
- Обновлен `persistence.ts` для поддержки версионирования (версия 2)
- Миграция автоматически применяется при загрузке диаграммы из localStorage
- Миграция применяется при импорте диаграммы из JSON файла
- Версия диаграммы обновляется до текущей после миграции

### Задачи:
1. **Создать миграцию для существующих канвасов**:
   - Найти все узлы типа `'rest'`, `'grpc'`, `'graphql'`, `'soap'`, `'websocket'`, `'webhook'`
   - Для каждого такого узла:
     - Найти все входящие соединения к нему
     - Для каждого соединения установить `connection.type` или `connection.data.protocol` = тип узла
     - Удалить узел протокола
     - Обновить целевой узел (если нужно)

2. **Создать `MigrationService`** (`src/utils/migration.ts`):
   ```typescript
   function migrateProtocolsToConnections(nodes: CanvasNode[], connections: CanvasConnection[]): {
     nodes: CanvasNode[];
     connections: CanvasConnection[];
   }
   ```

3. **Применить миграцию при загрузке канваса**:
   - В `useCanvasStore` при загрузке данных проверять версию схемы
   - Если версия < текущей, применять миграцию

### Файлы для создания:
- `src/utils/migration.ts` - миграция протоколов в соединения

---

## Этап 8: Обновление документации и примеров ✅ ВЫПОЛНЕНО

### ✅ Статус: ЗАВЕРШЕНО
- Добавлен раздел "Протоколы и API" в `docs/README.md` с описанием новой концепции
- Документированы все поддерживаемые протоколы (REST, GraphQL, SOAP, gRPC, WebSocket, Webhook)
- Описана настройка протоколов в UI
- Описана визуализация протоколов на канвасе
- Добавлена информация о миграции существующих диаграмм
- Примеры конфигураций (`enterprise_architecture_demo.json`) будут автоматически мигрированы при загрузке

### Задачи:
1. **Обновить документацию**:
   - Объяснить новую концепцию протоколов как атрибутов соединений
   - Показать примеры использования

2. **Обновить примеры конфигураций**:
   - `enterprise_architecture_demo.json` - убрать узлы протоколов, добавить протоколы в соединения

### Файлы для изменения:
- `docs/README.md` - обновить документацию
- `enterprise_architecture_demo.json` - мигрировать примеры (миграция применяется автоматически при загрузке)

---

## Порядок выполнения этапов

### Фаза 1: Подготовка (Этапы 1-2)
1. Расширить типы для поддержки протоколов в соединениях
2. Удалить протоколы из COMPONENT_LIBRARY

### Фаза 2: Ядро (Этапы 3-4)
3. Рефакторинг DataFlowEngine
4. Рефакторинг EmulationEngine

### Фаза 3: Интеграция (Этапы 5-6)
5. Рефакторинг Connection Rules
6. Рефакторинг UI

### Фаза 4: Миграция (Этапы 7-8)
7. Миграция существующих данных
8. Обновление документации

---

## Риски и сложности

1. **Обратная совместимость**:
   - Существующие канвасы могут сломаться
   - Нужна миграция данных

2. **Сложность рефакторинга**:
   - Много мест, где используются протоколы как узлы
   - Нужно аккуратно переделать логику

3. **UI изменения**:
   - Пользователи привыкли к протоколам как узлам
   - Нужно объяснить новую концепцию

4. **Тестирование**:
   - Нужно протестировать все сценарии использования протоколов

---

## Критерии успеха

1. ✅ Протоколы больше не отображаются как отдельные узлы в библиотеке компонентов
2. ✅ Протоколы настраиваются на уровне соединений
3. ✅ Метрики протоколов учитываются в метриках соединений
4. ✅ Существующие канвасы мигрируются без потери данных
5. ✅ Integration компоненты (Kong, MuleSoft, BFF) работают с протоколами через соединения
6. ✅ Документация обновлена с описанием новой концепции
7. ✅ Миграция применяется автоматически при загрузке диаграмм

---

## Время выполнения (оценка)

- **Фаза 1**: 2-3 часа
- **Фаза 2**: 4-6 часов
- **Фаза 3**: 3-4 часа
- **Фаза 4**: 2-3 часа

**Итого**: ~12-16 часов работы

---

## 📚 Дополнительный контекст для работы

### Ключевые файлы и их роль

#### Типы и интерфейсы
- **`src/types/index.ts`**: Основные типы для узлов и соединений
  - `CanvasNode` - узел на канвасе
  - `CanvasConnection` - соединение между узлами
  - `ConnectionConfig` - конфигурация соединения (сетевые параметры + протоколы)

- **`src/services/connection/types.ts`**: Типы для connection rules
  - `ConnectionMetadata` - метаданные соединения (протокол, хосты, порты)
  - `ConnectionRule` - правило автоматической конфигурации при создании соединения

#### Ядро обработки данных
- **`src/core/DataFlowEngine.ts`**: Обработка потока данных через узлы и соединения
  - `processData()` - обработка сообщения в узле
  - `processConnection()` - обработка сообщения при прохождении через соединение
  - `ComponentDataHandler` - интерфейс обработчика для типа компонента

- **`src/core/EmulationEngine.ts`**: Симуляция метрик компонентов
  - `updateComponentMetrics()` - обновление метрик узла
  - `simulateAPI()`, `simulateSOAP()`, etc. - симуляции для протоколов (нужно удалить)

#### Connection Rules
- **`src/services/connection/rules/`**: Автоматическая конфигурация при создании соединений
  - `kongRules.ts` - Kong → Backend
  - `bffRules.ts` - BFF → Backend
  - `mulesoftRules.ts` - MuleSoft → Backend
  - `graphqlGatewayRules.ts` - GraphQL Gateway → GraphQL Service

- **`src/services/connection/ServiceDiscovery.ts`**: Определение метаданных соединения
  - `getConnectionMetadata()` - определение протокола, хостов, портов

#### UI
- **`src/components/config/api/`**: Конфиги для протоколов (нужно удалить)
- **`src/components/config/ComponentConfigRenderer.tsx`**: Рендеринг конфигов компонентов
- **`src/components/canvas/ConnectionLine.tsx`**: Визуализация соединений на канвасе

---

### Примеры использования до и после

#### Пример 1: CRM → Payment Gateway

**ДО (неправильно):**
```typescript
// Узлы на канвасе
const nodes = [
  { id: 'crm-1', type: 'crm', ... },
  { id: 'rest-1', type: 'rest', ... },  // ❌ Отдельный узел
  { id: 'payment-1', type: 'payment-gateway', ... },
];

// Соединения
const connections = [
  { source: 'crm-1', target: 'rest-1', type: 'async' },
  { source: 'rest-1', target: 'payment-1', type: 'async' },
];
```

**ПОСЛЕ (правильно):**
```typescript
// Узлы на канвасе
const nodes = [
  { id: 'crm-1', type: 'crm', ... },
  { id: 'payment-1', type: 'payment-gateway', ... },
];

// Соединение с протоколом
const connections = [
  { 
    source: 'crm-1', 
    target: 'payment-1', 
    type: 'rest',  // ✅ Протокол на соединении
    data: {
      protocol: 'rest',
      protocolConfig: {
        httpMethod: 'POST',
        contentType: 'json',
      },
      latencyMs: 50,
    }
  },
];
```

#### Пример 2: BFF → Multiple Backends

**ДО (неправильно):**
```typescript
const nodes = [
  { id: 'bff-1', type: 'bff-service', ... },
  { id: 'rest-1', type: 'rest', ... },      // ❌
  { id: 'grpc-1', type: 'grpc', ... },     // ❌
  { id: 'user-svc', type: 'crm', ... },
  { id: 'order-svc', type: 'erp', ... },
];

const connections = [
  { source: 'bff-1', target: 'rest-1', type: 'async' },
  { source: 'rest-1', target: 'user-svc', type: 'async' },
  { source: 'bff-1', target: 'grpc-1', type: 'async' },
  { source: 'grpc-1', target: 'order-svc', type: 'async' },
];
```

**ПОСЛЕ (правильно):**
```typescript
const nodes = [
  { id: 'bff-1', type: 'bff-service', ... },
  { id: 'user-svc', type: 'crm', ... },
  { id: 'order-svc', type: 'erp', ... },
];

const connections = [
  { 
    source: 'bff-1', 
    target: 'user-svc', 
    type: 'rest',  // ✅ REST протокол
    data: { protocol: 'rest', ... }
  },
  { 
    source: 'bff-1', 
    target: 'order-svc', 
    type: 'grpc',  // ✅ gRPC протокол
    data: { protocol: 'grpc', ... }
  },
];
```

---

### Важные принципы при реализации

1. **Обратная совместимость**: Старые канвасы должны работать (через миграцию)
2. **Постепенность**: Не ломать все сразу, делать по этапам
3. **Тестирование**: Проверять каждый этап перед переходом к следующему
4. **Документация**: Обновлять документацию параллельно с кодом

---

## Следующие шаги

1. ✅ План создан и дополнен контекстом
2. ⏳ Подтвердить план с пользователем
3. ⏳ Начать с Фазы 1 (расширение типов)
4. ⏳ Постепенно переходить к следующим фазам
5. ⏳ Тестировать на каждом этапе
