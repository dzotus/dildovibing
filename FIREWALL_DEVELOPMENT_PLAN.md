# План разработки компонента Firewall

## Обзор

Этот документ описывает план доработки компонента Network Firewall для соответствия реальной архитектуре сетевых файрволов и улучшения симулятивности системы.

## Текущее состояние

### Что уже реализовано:

1. **FirewallEmulationEngine** (`src/core/FirewallEmulationEngine.ts`)
   - Базовая обработка пакетов
   - Симуляция пакетов для метрик
   - Интеграция с FirewallRoutingEngine
   - Расчет метрик (throughput, latency, error rate, utilization)

2. **FirewallRoutingEngine** (`src/core/FirewallRoutingEngine.ts`)
   - Обработка правил фильтрации
   - Stateful inspection (базовая реализация)
   - Connection tracking
   - CIDR matching
   - Логирование пакетов

3. **FirewallConfigAdvanced** (`src/components/config/security/FirewallConfigAdvanced.tsx`)
   - UI для управления правилами
   - Настройки файрвола
   - Просмотр логов
   - Статистика в реальном времени

4. **Интеграция с DataFlowEngine**
   - Handler для обработки сообщений через файрвол
   - Извлечение информации о пакетах из сообщений

5. **Интеграция с EmulationEngine**
   - Инициализация движка
   - Симуляция метрик
   - Расчет нагрузки

### Что отсутствует или требует доработки:

1. ~~**Connection Rules** - нет правил подключения для Firewall~~ ✅ **ВЫПОЛНЕНО** - созданы правила подключения
2. ~~**Реалистичная симуляция** - некоторые аспекты не соответствуют реальности~~ ✅ **ВЫПОЛНЕНО** - убран хардкод, используются реальные данные
3. ~~**Stateful inspection** - упрощенная реализация, не учитывает все состояния TCP~~ ✅ **ВЫПОЛНЕНО** - реализованы полные TCP states, UDP и ICMP tracking
4. ~~**Интеграция с DataFlowEngine** - улучшение извлечения информации о пакетах~~ ✅ **ВЫПОЛНЕНО** - добавлено извлечение TCP flags, ICMP type/code, bytes, timestamp
5. **Интеграция с сетевой моделью** - частично выполнено, можно улучшить (опционально)
6. **Метрики производительности** - базовые метрики есть, можно добавить больше (CPU, Memory, Connection table size) - опционально

---

## Шаг 1: Изучение архитектуры системы

### Файлы для изучения:

1. **`src/core/EmulationEngine.ts`**
   - Метод `initializeFirewallEngine()` (строка ~10161)
   - Метод `simulateFirewall()` (строка ~7267)
   - Как инициализируются другие security компоненты (WAF, IDS/IPS)

2. **`src/core/DataFlowEngine.ts`**
   - Метод `createFirewallHandler()` (строка ~6328)
   - Как обрабатываются сообщения через файрвол
   - Как извлекается информация о пакетах

3. **`src/services/connection/ServiceDiscovery.ts`**
   - Как разрешаются имена и порты компонентов
   - Как работает сетевой стек

4. **`src/services/connection/rules/wafRules.ts`**
   - Пример правил подключения для security компонента
   - Как настраиваются автоматические конфигурации

5. **`src/core/WAFEmulationEngine.ts`** (для сравнения)
   - Как реализован похожий security компонент
   - Паттерны симуляции

### Похожие компоненты для изучения:

- **WAF** (`src/core/WAFEmulationEngine.ts`, `src/services/connection/rules/wafRules.ts`)
  - Похожий security компонент
  - Обработка сетевого трафика
  - Правила подключения

- **IDS/IPS** (`src/core/IDSIPSEmulationEngine.ts`)
  - Анализ пакетов
  - Обнаружение аномалий

- **Load Balancer** (`src/core/NginxEmulationEngine.ts`, `src/services/connection/rules/loadBalancerRules.ts`)
  - Сетевая маршрутизация
  - Обработка соединений

---

## Шаг 2: Изучение реальной архитектуры Firewall

### Ключевые концепции реальных файрволов:

1. **Stateful Inspection**
   - Отслеживание состояний TCP (NEW, ESTABLISHED, RELATED, INVALID)
   - Отслеживание UDP сессий
   - Отслеживание ICMP сессий
   - Timeout для соединений
   - NAT tracking

2. **Packet Processing Pipeline**
   - PREROUTING → FORWARD → POSTROUTING (transit traffic)
   - PREROUTING → INPUT (incoming traffic)
   - OUTPUT → POSTROUTING (outgoing traffic)
   - Правила применяются на разных этапах

3. **Rule Evaluation**
   - Приоритет правил
   - Первое совпадение останавливает проверку
   - Default policy для несовпадающих пакетов

4. **Connection Tracking**
   - Хранение состояния соединений
   - Очистка старых соединений
   - Ограничение количества соединений

5. **Performance Metrics**
   - Packets per second (PPS)
   - Connections per second (CPS)
   - Latency (микросекунды для hardware, миллисекунды для software)
   - CPU utilization
   - Memory usage
   - Connection table size

### Ресурсы для изучения:

- Netfilter/iptables architecture
- AWS Network Firewall documentation
- pfSense/OPNsense architecture
- Linux netfilter conntrack

---

## Шаг 3: Создание Connection Rules для Firewall

### Файл: `src/services/connection/rules/firewallRules.ts`

**Цель:** Определить правила подключения компонентов к Firewall и через Firewall.

### Требования:

1. **Firewall как транзитный компонент**
   - Компоненты могут подключаться через Firewall
   - Firewall должен быть прозрачным для трафика
   - Автоматическая настройка маршрутизации

2. **Firewall как защитный барьер**
   - Компоненты могут подключаться к Firewall для защиты
   - Firewall должен фильтровать трафик
   - Настройка правил на основе подключений

3. **Интеграция с Load Balancers**
   - Firewall перед Load Balancer
   - Firewall после Load Balancer
   - Автоматическая настройка правил

4. **Интеграция с API Gateways**
   - Firewall перед API Gateway
   - Настройка правил для API endpoints

### Структура правил:

```typescript
export function createFirewallRule(discovery: ServiceDiscovery): ConnectionRule {
  return {
    sourceType: '*', // Любой компонент может отправлять через Firewall
    targetTypes: ['*'], // Firewall может защищать любые компоненты
    priority: 10, // Высокий приоритет для security
    
    // Обновление конфигурации источника (компонент перед Firewall)
    updateSourceConfig: (source, firewall, connection, metadata) => {
      // Настройка маршрутизации через Firewall
      // Добавление правил в Firewall на основе типа источника
    },
    
    // Обновление конфигурации цели (компонент после Firewall)
    updateTargetConfig: (firewall, target, connection, metadata) => {
      // Настройка правил Firewall для защиты цели
      // Автоматическое создание правил allow для целевого компонента
    },
    
    // Валидация подключения
    validateConnection: (source, target, connection) => {
      // Проверка что Firewall находится между компонентами
      // Валидация сетевой топологии
    }
  };
}
```

### Регистрация в `src/services/connection/rules/index.ts`:

Добавить:
```typescript
import { createFirewallRule } from './firewallRules';

// В функции initializeConnectionRules:
createFirewallRule(discovery),
```

---

## Шаг 4: Улучшение Stateful Inspection

### Файл: `src/core/FirewallRoutingEngine.ts`

### Текущие проблемы:

1. **Упрощенная логика состояний TCP**
   - Не учитываются все состояния (SYN, SYN-ACK, ACK, FIN, RST)
   - Нет проверки последовательности пакетов

2. **UDP tracking**
   - UDP не имеет состояний, но файрволы отслеживают "сессии"
   - Нужна логика для UDP

3. **ICMP tracking**
   - ICMP сообщения связаны с соединениями
   - Нужна логика для ICMP

4. **Connection timeout**
   - Есть базовая очистка, но нужно улучшить логику

### Улучшения:

1. **Расширить ConnectionState interface:**
```typescript
interface ConnectionState {
  source: string;
  destination: string;
  sourcePort: number;
  destinationPort: number;
  protocol: 'tcp' | 'udp' | 'icmp';
  state: 'new' | 'syn-sent' | 'syn-received' | 'established' | 'fin-wait-1' | 'fin-wait-2' | 'close-wait' | 'closing' | 'last-ack' | 'time-wait' | 'closed' | 'related' | 'invalid';
  tcpFlags?: {
    syn?: boolean;
    ack?: boolean;
    fin?: boolean;
    rst?: boolean;
  };
  firstSeen: number;
  lastSeen: number;
  packets: number;
  bytes: number;
  direction: 'original' | 'reply';
}
```

2. **Улучшить логику обработки TCP:**
   - Проверка флагов TCP
   - Отслеживание handshake
   - Обработка FIN/RST пакетов
   - Timeout для разных состояний

3. **Добавить UDP session tracking:**
   - Отслеживание UDP "сессий" по паре IP:port
   - Timeout для UDP сессий (обычно 30-60 секунд)

4. **Добавить ICMP tracking:**
   - Связывание ICMP ответов с запросами
   - Отслеживание ICMP ошибок для TCP/UDP

5. **Улучшить cleanup:**
   - Разные timeout для разных протоколов
   - Приоритетная очистка старых соединений
   - Ограничение размера таблицы соединений

---

## Шаг 5: Улучшение симуляции пакетов

### Файл: `src/core/FirewallEmulationEngine.ts`

### Текущие проблемы:

1. **Генерация случайных пакетов**
   - Слишком случайная генерация
   - Не учитывается реальная топология сети

2. **Оценка PPS**
   - Простое суммирование throughput
   - Не учитывается размер пакетов

3. **Латентность**
   - Фиксированная латентность
   - Не учитывается нагрузка

### Улучшения:

1. **Умная генерация пакетов:**
   - Использовать реальные IP адреса из подключенных компонентов
   - Генерировать пакеты на основе реальных соединений
   - Учитывать протоколы из правил

2. **Реалистичная латентность:**
   - Базовая латентность: 0.1-0.5ms (hardware) или 1-5ms (software)
   - Увеличение при высокой нагрузке
   - Увеличение при большом количестве правил
   - Увеличение при stateful inspection

3. **Улучшенная оценка PPS:**
   - Учитывать размер пакетов
   - Разные PPS для разных протоколов
   - Учитывать burst traffic

4. **Реалистичные метрики:**
   - CPU utilization на основе PPS и количества правил
   - Memory usage на основе размера таблицы соединений
   - Connection table utilization

---

## Шаг 6: Улучшение интеграции с DataFlowEngine ✅ **ВЫПОЛНЕНО**

### Файл: `src/core/DataFlowEngine.ts`

### Реализовано:

1. **Улучшено извлечение информации о пакетах:**
   - ✅ Извлечение TCP flags (syn, ack, fin, rst, psh, urg) из payload и metadata
   - ✅ Извлечение ICMP type и code из payload и metadata
   - ✅ Извлечение bytes из сообщений для расчета метрик
   - ✅ Извлечение timestamp из сообщений
   - ✅ Поддержка разных форматов (JSON, XML, binary)
   - ✅ Fallback на значения по умолчанию только если нет данных в сообщении

2. **Обработка метаданных:**
   - ✅ Использование metadata из ConnectionConfig
   - ✅ Использование metadata из DataMessage
   - ✅ Передача всех данных в FirewallRoutingEngine для полной поддержки stateful inspection

3. **Логирование:**
   - ✅ Логирование всех блокировок через FirewallRoutingEngine
   - ✅ Добавление информации о проверке Firewall в metadata сообщений

---

## Шаг 7: Улучшение UI/UX

### Файл: `src/components/config/security/FirewallConfigAdvanced.tsx`

### Текущие проблемы:

1. **Отображение метрик**
   - Не все метрики отображаются
   - Нет графиков производительности

2. **Управление правилами**
   - Нет группировки правил
   - Нет экспорта/импорта правил
   - Нет тестирования правил

3. **Логи**
   - Нет фильтрации по времени
   - Нет экспорта логов
   - Нет детального просмотра пакетов

### Улучшения:

1. **Добавить графики:**
   - График PPS в реальном времени
   - График латентности
   - График блокировок
   - График использования CPU/Memory

2. **Улучшить управление правилами:**
   - Группировка правил по категориям
   - Drag-and-drop для изменения приоритета
   - Тестирование правил на примерах пакетов
   - Экспорт/импорт правил (JSON, CSV)

3. **Улучшить логи:**
   - Фильтрация по времени (последний час, день, неделя)
   - Поиск по содержимому
   - Экспорт логов
   - Детальный просмотр пакета (hex dump)

4. **Добавить визуализацию:**
   - Карта сети с показом блокировок
   - Топ источников трафика
   - Топ заблокированных IP

---

## Шаг 8: Добавление дополнительных функций

### Функции реальных файрволов:

1. **Intrusion Detection/Prevention**
   - Базовая реализация есть, но нужно улучшить
   - Добавить сигнатуры атак
   - Добавить обнаружение аномалий

2. **NAT (Network Address Translation)**
   - SNAT (Source NAT)
   - DNAT (Destination NAT)
   - Port forwarding

3. **VPN Passthrough**
   - Поддержка VPN туннелей
   - Интеграция с VPN компонентом

4. **Rate Limiting**
   - Ограничение скорости соединений
   - Защита от DDoS

5. **Geo-blocking**
   - Блокировка по географическому расположению
   - Whitelist/blacklist стран

### Приоритет реализации:

1. **Высокий приоритет:**
   - Улучшение Stateful Inspection
   - Connection Rules
   - Улучшение симуляции

2. **Средний приоритет:**
   - Улучшение UI/UX
   - Дополнительные метрики
   - NAT (если нужен)

3. **Низкий приоритет:**
   - Geo-blocking
   - Расширенный IDS/IPS
   - VPN Passthrough

---

## Шаг 9: Тестирование и валидация

### Тестовые сценарии:

1. **Базовое функционирование:**
   - Создание правил
   - Блокировка/разрешение трафика
   - Просмотр логов

2. **Stateful Inspection:**
   - TCP handshake
   - Established connections
   - Connection timeout

3. **Производительность:**
   - Высокая нагрузка (10000+ PPS)
   - Много правил (100+)
   - Много соединений (1000+)

4. **Интеграция:**
   - Подключение через Firewall
   - Firewall перед Load Balancer
   - Firewall перед API Gateway

5. **Граничные случаи:**
   - Нет правил (default policy)
   - Все правила disabled
   - Firewall disabled
   - Переполнение таблицы соединений

---

## Шаг 10: Документация

### Что задокументировать:

1. **Архитектура компонента**
   - Как работает Firewall в системе
   - Как интегрируется с другими компонентами
   - Как обрабатываются пакеты

2. **Конфигурация**
   - Описание всех параметров
   - Примеры правил
   - Best practices

3. **Метрики**
   - Описание всех метрик
   - Как интерпретировать метрики
   - Нормальные значения

4. **Troubleshooting**
   - Частые проблемы
   - Как диагностировать проблемы
   - Как оптимизировать производительность

---

## Чеклист реализации

### Фаза 1: Базовая доработка (обязательно)

- [x] Изучить архитектуру системы (EmulationEngine, DataFlowEngine, ServiceDiscovery) ✅ **ВЫПОЛНЕНО**
- [x] Изучить похожие компоненты (WAF, IDS/IPS) ✅ **ВЫПОЛНЕНО**
- [x] Создать Connection Rules для Firewall ✅ **ВЫПОЛНЕНО** - создан `src/services/connection/rules/firewallRules.ts`
- [x] Зарегистрировать правила в index.ts ✅ **ВЫПОЛНЕНО** - добавлен `createFirewallRule(discovery)` в `initializeConnectionRules`
- [x] Улучшить Stateful Inspection (TCP states, UDP, ICMP) ✅ **ВЫПОЛНЕНО** - расширен `ConnectionState` с полными TCP states, добавлен UDP и ICMP tracking
- [x] Улучшить симуляцию пакетов (реалистичные IP, латентность) ✅ **ВЫПОЛНЕНО** - убран хардкод, используются реальные данные из правил и подключенных компонентов, реалистичная латентность
- [x] Улучшить интеграцию с DataFlowEngine ✅ **ВЫПОЛНЕНО** - улучшено извлечение информации о пакетах, добавлена поддержка TCP flags (syn, ack, fin, rst, psh, urg), ICMP type/code, bytes для расчета метрик, timestamp
- [ ] Протестировать базовое функционирование ⏳ **ОСТАЛОСЬ** - требует ручного тестирования

### Фаза 2: Улучшение UI/UX (желательно)

- [ ] Добавить графики метрик
- [ ] Улучшить управление правилами (группировка, drag-and-drop)
- [ ] Улучшить логи (фильтрация, экспорт)
- [ ] Добавить визуализацию сети

### Фаза 3: Дополнительные функции (опционально)

- [ ] NAT (если нужен)
- [ ] Rate Limiting
- [ ] Geo-blocking
- [ ] Расширенный IDS/IPS

### Фаза 4: Документация и тестирование

- [ ] Написать документацию
- [ ] Создать тестовые сценарии
- [ ] Провести нагрузочное тестирование
- [ ] Исправить найденные проблемы

---

## Примечания для разработчика

1. **Не копировать слепо другие компоненты**
   - Каждый компонент уникален
   - Firewall имеет свою специфику (packet-level processing, stateful inspection)
   - Использовать паттерны, но адаптировать под Firewall

2. **Соответствие реальности**
   - Изучать реальные файрволы (iptables, pfSense, AWS Network Firewall)
   - Учитывать реальные метрики производительности
   - Учитывать реальные ограничения

3. **Избегать хардкода**
   - Все параметры должны быть конфигурируемыми
   - Использовать значения по умолчанию из реальных файрволов
   - Позволять пользователю настраивать все аспекты

4. **Производительность**
   - Firewall обрабатывает много пакетов
   - Оптимизировать алгоритмы сопоставления правил
   - Использовать эффективные структуры данных

5. **Безопасность**
   - Firewall - критический компонент безопасности
   - Валидировать все входные данные
   - Обрабатывать ошибки gracefully
   - Логировать все важные события

---

## Следующие шаги

После завершения этого плана:

1. Провести code review
2. Протестировать в различных сценариях
3. Собрать feedback от пользователей
4. Итеративно улучшать на основе feedback

---

## Контакты и вопросы

При возникновении вопросов:
- Изучить существующий код похожих компонентов
- Проверить документацию в `docs/security/`
- Изучить реальную архитектуру файрволов

---

## Текущий статус разработки (обновлено после версии 0.1.8z)

### ✅ Фаза 1: Базовая доработка - ЗАВЕРШЕНА

Все обязательные задачи выполнены:
- ✅ Connection Rules созданы и зарегистрированы
- ✅ Stateful Inspection полностью реализован (TCP, UDP, ICMP)
- ✅ Устранен весь хардкод, используются только реальные данные
- ✅ Реалистичная симуляция с учетом всех факторов
- ✅ Улучшена интеграция с DataFlowEngine (TCP flags, ICMP, bytes, timestamp)

**Осталось:**
- ⏳ Ручное тестирование базового функционирования (требует тестирования в UI)

### 📋 Фаза 2: Улучшение UI/UX - НЕ НАЧАТА (опционально)

Задачи для будущих версий:
- [ ] Добавить графики метрик (PPS, latency, блокировки, CPU/Memory)
- [ ] Улучшить управление правилами (группировка, drag-and-drop)
- [ ] Улучшить логи (фильтрация по времени, поиск, экспорт)
- [ ] Добавить визуализацию сети (карта блокировок, топ источников)

### 📋 Фаза 3: Дополнительные функции - НЕ НАЧАТА (опционально)

Задачи для будущих версий:
- [ ] NAT (Network Address Translation) - если потребуется
- [ ] Rate Limiting для защиты от DDoS
- [ ] Geo-blocking для блокировки по странам
- [ ] Расширенный IDS/IPS с сигнатурами атак

### 📋 Фаза 4: Документация и тестирование - НЕ НАЧАТА (опционально)

Задачи для будущих версий:
- [ ] Написать документацию по архитектуре компонента
- [ ] Создать тестовые сценарии
- [ ] Провести нагрузочное тестирование
- [ ] Исправить найденные проблемы

---

## Резюме

**Версия 0.1.8z**: Все критические улучшения реализованы. Компонент Firewall полностью функционален с реалистичной симуляцией, полной поддержкой stateful inspection и отсутствием хардкода. Готов к использованию и тестированию.

**Следующие шаги**: Ручное тестирование в UI для проверки базового функционирования. После тестирования можно переходить к опциональным улучшениям (Фазы 2-4) при необходимости.
