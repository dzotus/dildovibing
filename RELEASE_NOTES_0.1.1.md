# Release Notes v0.1.1

## 🎨 Улучшения UI/UX

### Компактное правое меню (PropertiesPanel)

Полностью переработан правый панель свойств для лучшего использования пространства:

- **Адаптивная ширина**: Автоматически подстраивается под размер экрана
  - Маленькие экраны: 224px (w-56)
  - Средние экраны: 256px (w-64)
  - Большие экраны: 288px (w-72)
- **Компактные табы**: Уменьшены размеры и отступы для экономии места
  - Высота табов: 24px (h-6)
  - Размер текста: 9px
  - Сокращенные названия: "Diagnostics" → "Diag", "Properties" → "Props"
- **Оптимизированные отступы**: Уменьшены padding и spacing во всех компонентах
- **Компактные элементы**: Все внутренние компоненты (ComponentStateControl, ConnectionPropertiesPanel, GroupPropertiesPanel) стали более компактными

### Улучшенное контекстное меню для групп

Расширенная функциональность управления группами компонентов:

- **Поддержка множественных групп**: Компоненты теперь могут принадлежать к нескольким группам одновременно
- **Умное меню "Remove from Group"**:
  - Если компонент в 1 группе: сразу удаляется из группы
  - Если компонент в 2+ группах: открывается диалог выбора группы для удаления
- **Улучшенное меню "Add to Group"**:
  - Показывает только группы, в которых компонент еще не находится
  - Если компонент уже во всех группах, показывается сообщение "Already in all groups"
- **Закрытие диалогов**: Все диалоги управления группами закрываются при клике вне меню или нажатии Escape

## 🔧 Технические улучшения

### Очистка проекта

- Удалены случайно созданные файлы (`erssssDownloadsgovnozadengi`, `et GIT_PAGER=cat`)
- Обновлен `.gitignore`:
  - Добавлена папка `.cursor/` для конфигурации Cursor IDE
  - Добавлены паттерны для предотвращения случайных файлов

## 📝 Детали изменений

### Новые компоненты
- `RemoveFromGroupDialog` - диалог для удаления компонента из нескольких групп

### Обновленные компоненты
- `PropertiesPanel` - полностью переработан для компактности
- `ContextMenu` - добавлена поддержка `onRemoveFromGroup`
- `AddToGroupDialog` - улучшена фильтрация доступных групп
- `ComponentStateControl` - компактный дизайн
- `ConnectionPropertiesPanel` - оптимизированы отступы и размеры
- `GroupPropertiesPanel` - уменьшены размеры элементов

### Улучшения в Canvas.tsx
- Добавлена логика определения количества групп компонента
- Умная обработка удаления из групп (прямое удаление или диалог выбора)
- Поддержка множественных групп для компонентов

## 🐛 Исправления

- Исправлена логика показа кнопки "Remove from Group" (теперь показывается для всех компонентов в группах)
- Исправлена фильтрация групп в меню "Add to Group"

## 📦 Установка и обновление

```bash
# Переключиться на новую версию
git checkout release/0.1.1

# Установить зависимости (если нужно)
npm install

# Запустить проект
npm run dev
```

## 🔗 Ссылки

- GitHub: https://github.com/dzotus/dildovibing
- Бранч: `release/0.1.1`
- Предыдущая версия: `release/0.1.0`

---

**Дата релиза**: 2024-12-XX  
**Версия**: 0.1.1

