# GitLab CI/CD - Документация компонента

## Обзор

GitLab CI/CD - это встроенная система непрерывной интеграции и развертывания (CI/CD) в GitLab. Компонент GitLab CI в системе симуляции полностью эмулирует поведение реального GitLab CI/CD, включая управление pipelines, jobs, runners, stages, artifacts, cache, variables, environments, schedules, поддержку YAML конфигураций, rules, needs, retry, manual jobs, интеграции с другими компонентами и полный набор метрик производительности.

### Основные возможности

- ✅ **Pipeline Management** - Управление CI/CD pipelines с автоматическим вычислением статусов и IID (Internal ID)
- ✅ **Job Management** - Симуляция jobs с прогрессом, stages, длительностью и результатами
- ✅ **Runner Management** - Управление GitLab Runners (docker, kubernetes, shell executors)
- ✅ **Stage Management** - Управление stages с последовательным и параллельным выполнением
- ✅ **Artifact Management** - Управление артефактами с expiry и retention policy
- ✅ **Cache Management** - Симуляция кэширования с hit/miss метриками
- ✅ **Variable Management** - Управление CI/CD переменными (env_var, file, protected, masked)
- ✅ **Environment Management** - Управление окружениями развертывания
- ✅ **Schedule Management** - Управление scheduled pipelines с cron выражениями
- ✅ **YAML Import** - Импорт и парсинг .gitlab-ci.yml файлов
- ✅ **Rules Support** - Поддержка rules для условного выполнения jobs
- ✅ **Needs Support** - Поддержка needs для зависимостей между jobs
- ✅ **Retry Support** - Поддержка retry для failed jobs
- ✅ **Manual Jobs** - Поддержка manual jobs с ручным запуском
- ✅ **Pipeline Sources** - Поддержка различных источников pipelines (push, web, trigger, schedule, api)
- ✅ **Parent/Child Pipelines** - Поддержка parent и child pipelines
- ✅ **Merge Request Pipelines** - Поддержка pipelines для merge requests
- ✅ **Интеграции** - Интеграция с Prometheus, Loki, Jaeger, Kubernetes, S3, PagerDuty
- ✅ **Метрики GitLab CI** - Полный набор метрик производительности и использования

---

## Основные функции

### 1. Pipeline Management (Управление pipelines)

**Описание:** GitLab CI управляет CI/CD pipelines для автоматизации процессов сборки, тестирования и развертывания.

**Структура Pipeline Template (из конфига):**
```json
{
  "id": "pipeline-template-1",
  "ref": "main",
  "source": "push",
  "stages": [
    {
      "name": "build",
      "jobs": [
        {
          "name": "build-job",
          "stage": "build",
          "script": ["npm install", "npm run build"],
          "image": "node:18",
          "tags": ["docker"],
          "when": "on_success",
          "allowFailure": false
        }
      ]
    }
  ],
  "resultDestinations": [
    {
      "targetComponentId": "prometheus-1",
      "format": "prometheus",
      "condition": "always"
    }
  ]
}
```

**Структура Pipeline Execution (реальное выполнение):**
```json
{
  "id": "pipeline-execution-123",
  "templateId": "pipeline-template-1",
  "iid": 42,
  "retryOf": null,
  "status": "running",
  "ref": "main",
  "sha": "abc123def456789...",
  "source": "push",
  "stages": [
    {
      "name": "build",
      "status": "running",
      "jobs": [...],
      "startTime": 1609459200000,
      "duration": 120000
    }
  ],
  "createdAt": 1609459200000,
  "startedAt": 1609459201000,
  "finishedAt": null,
  "duration": null,
  "queuedDuration": 1000,
  "coverage": 85.5,
  "variables": {
    "CI_COMMIT_REF_NAME": "main",
    "CI_COMMIT_SHA": "abc123..."
  }
}
```

**Параметры Pipeline Template:**
- **id** - Уникальный идентификатор template (обязательно)
- **ref** - Ветка или тег (опционально, по умолчанию: `main`)
- **source** - Источник pipeline: `push`, `web`, `trigger`, `schedule`, `api` (по умолчанию: `push`)
- **stages** - Массив stages с jobs (обязательно)
- **resultDestinations** - Массив destinations для отправки результатов (опционально)

**Параметры Pipeline Execution:**
- **id** - Уникальный идентификатор execution (генерируется автоматически)
- **templateId** - Ссылка на template (обязательно)
- **iid** - Internal ID (project-level, последовательный номер, увеличивается только при создании нового execution)
- **retryOf** - IID pipeline, который retry (опционально, только для retry pipelines)
- **status** - Статус: `created`, `waiting_for_resource`, `preparing`, `pending`, `running`, `success`, `failed`, `canceled`, `skipped`, `manual`, `scheduled`
- **ref** - Ветка или тег
- **sha** - Commit SHA (опционально)
- **source** - Источник pipeline
- **stages** - Массив stages с jobs
- **createdAt** - Время создания (timestamp)
- **startedAt** - Время начала выполнения (timestamp, опционально)
- **finishedAt** - Время завершения (timestamp, опционально)
- **duration** - Длительность выполнения в миллисекундах (опционально)
- **queuedDuration** - Длительность ожидания в очереди в миллисекундах
- **coverage** - Code coverage процент (опционально)
- **variables** - CI/CD переменные (опционально)
- **mergeRequest** - Информация о merge request (опционально)
- **parentPipelineId** - ID parent pipeline (опционально, для child pipelines)
- **childPipelineIds** - Массив ID child pipelines (опционально, для parent pipelines)

**Статусы Pipeline:**
- **created** - Pipeline создан, но еще не запущен
- **waiting_for_resource** - Ожидает доступных ресурсов (runners)
- **preparing** - Подготовка к выполнению
- **pending** - Ожидает запуска
- **running** - Выполняется
- **success** - Успешно завершен
- **failed** - Провалился
- **canceled** - Отменен
- **skipped** - Пропущен (по правилам)
- **manual** - Ожидает ручного запуска
- **scheduled** - Запланирован по schedule

**Pipeline IID (Internal ID):**
- IID - это последовательный номер pipeline в проекте
- IID увеличивается только при создании нового execution (не при retry)
- Retry pipeline имеет тот же IID, что и оригинальный pipeline
- IID используется для идентификации pipeline в UI GitLab

**Pipeline Retry:**
- При retry создается новый execution с тем же Iid, что и оригинальный pipeline
- Поле `retryOf` содержит IID оригинального pipeline
- Retry pipeline имеет новый execution ID, но тот же IID

### 2. Job Management (Управление jobs)

**Описание:** GitLab CI выполняет jobs (задачи) в рамках pipelines.

**Структура Job:**
```json
{
  "id": "job-123",
  "name": "build-job",
  "stage": "build",
  "status": "running",
  "pipelineId": "pipeline-execution-123",
  "startTime": 1609459200000,
  "duration": 60000,
  "estimatedDuration": 60000,
  "progress": 75,
  "runnerId": "runner-1",
  "logs": [
    "$ npm install",
    "added 1234 packages in 30s",
    "$ npm run build",
    "> Building...",
    "> Build completed successfully"
  ],
  "artifacts": ["dist/app.js", "dist/app.css"],
  "variables": {
    "NODE_ENV": "production"
  },
  "when": "on_success",
  "allowFailure": false,
  "retry": 0,
  "maxRetries": 2,
  "tags": ["docker", "linux"],
  "image": "node:18",
  "services": ["postgres:13", "redis:6"],
  "script": ["npm install", "npm run build"],
  "beforeScript": ["echo 'Starting build'"],
  "afterScript": ["echo 'Build completed'"],
  "needs": [],
  "rules": [
    {
      "if": "$CI_COMMIT_BRANCH == 'main'",
      "when": "on_success"
    }
  ],
  "cache": {
    "key": "npm-cache-$CI_COMMIT_REF_SLUG",
    "paths": ["node_modules/"],
    "policy": "pull-push",
    "when": "on_success"
  },
  "dependencies": []
}
```

**Параметры Job:**
- **id** - Уникальный идентификатор (генерируется автоматически)
- **name** - Имя job (обязательно)
- **stage** - Stage, к которому принадлежит job (обязательно)
- **status** - Статус: `created`, `pending`, `running`, `success`, `failed`, `canceled`, `skipped`, `manual`
- **pipelineId** - ID pipeline execution (обязательно)
- **startTime** - Время начала выполнения (timestamp, опционально)
- **duration** - Длительность выполнения в миллисекундах (опционально)
- **estimatedDuration** - Оценка длительности в миллисекундах (опционально)
- **progress** - Прогресс выполнения (0-100, опционально)
- **runnerId** - ID runner, выполняющего job (опционально)
- **logs** - Логи выполнения (массив строк, опционально)
- **artifacts** - Артефакты, созданные job (массив путей, опционально)
- **variables** - CI/CD переменные для job (опционально)
- **when** - Условие выполнения: `on_success`, `on_failure`, `always`, `manual` (по умолчанию: `on_success`)
- **allowFailure** - Разрешить failure без остановки pipeline (по умолчанию: `false`)
- **retry** - Количество попыток retry (по умолчанию: `0`)
- **maxRetries** - Максимальное количество попыток retry (опционально)
- **tags** - Теги для выбора runner (массив строк, опционально)
- **image** - Docker image для job (опционально)
- **services** - Docker services для job (массив строк, опционально)
- **script** - Команды для выполнения (массив строк, обязательно)
- **beforeScript** - Команды перед script (массив строк, опционально)
- **afterScript** - Команды после script (массив строк, опционально)
- **needs** - Имена jobs, от которых зависит этот job (массив строк, опционально)
- **rules** - Правила для условного выполнения (массив GitLabCIRule, опционально)
- **cache** - Конфигурация кэша (опционально)
- **dependencies** - Имена jobs, artifacts которых нужны (массив строк, опционально)
- **integrations** - Интеграции с другими компонентами (опционально)

**Статусы Job:**
- **created** - Job создан, но еще не запущен
- **pending** - Ожидает доступного runner
- **running** - Выполняется
- **success** - Успешно завершен
- **failed** - Провалился
- **canceled** - Отменен
- **skipped** - Пропущен (по правилам или needs)
- **manual** - Ожидает ручного запуска

**Job Rules:**
- Rules определяют условия выполнения job
- Поддерживаются выражения с CI переменными (например, `$CI_COMMIT_BRANCH == 'main'`)
- Правила проверяются последовательно, первое совпадение определяет поведение
- Если ни одно правило не совпало, job пропускается (skipped)

**Job Needs:**
- Needs определяет зависимости между jobs
- Job с needs может начать выполнение до завершения stage, если его зависимости выполнены
- Позволяет создавать граф зависимостей вместо строгой последовательности stages

**Job Retry:**
- При failure job может быть автоматически retry
- Количество попыток retry настраивается через `maxRetries`
- Retry создает новый job execution с тем же именем

**Manual Jobs:**
- Jobs с `when: "manual"` требуют ручного запуска
- Manual jobs не блокируют pipeline, но pipeline остается в статусе `manual` до запуска
- Manual jobs могут быть запущены через UI или API

### 3. Stage Management (Управление stages)

**Описание:** GitLab CI группирует jobs в stages для последовательного выполнения.

**Структура Stage:**
```json
{
  "name": "build",
  "status": "running",
  "jobs": [
    {
      "id": "job-1",
      "name": "build-job",
      "status": "success"
    },
    {
      "id": "job-2",
      "name": "test-job",
      "status": "running"
    }
  ],
  "startTime": 1609459200000,
  "duration": 120000
}
```

**Параметры Stage:**
- **name** - Имя stage (обязательно)
- **status** - Статус: `pending`, `running`, `success`, `failed`, `canceled`, `skipped`
- **jobs** - Массив jobs в stage (обязательно)
- **startTime** - Время начала выполнения (timestamp, опционально)
- **duration** - Длительность выполнения в миллисекундах (опционально)

**Статусы Stage:**
- **pending** - Ожидает выполнения
- **running** - Выполняется (хотя бы один job running)
- **success** - Все jobs успешны
- **failed** - Хотя бы один job провалился (и allowFailure=false)
- **canceled** - Stage отменен
- **skipped** - Все jobs пропущены

**Выполнение Stages:**
- Stages выполняются последовательно
- Следующий stage начинается только после успешного завершения предыдущего
- Jobs в одном stage могут выполняться параллельно (если есть доступные runners)
- Jobs с needs могут начать выполнение до завершения stage

### 4. Runner Management (Управление runners)

**Описание:** GitLab CI использует runners для выполнения jobs.

**Структура Runner:**
```json
{
  "id": "runner-1",
  "name": "docker-runner-1",
  "description": "Docker runner for Linux builds",
  "status": "online",
  "runnerType": "project_type",
  "executor": "docker",
  "platform": "linux",
  "architecture": "amd64",
  "active": true,
  "isShared": false,
  "locked": false,
  "maximumTimeout": 3600,
  "tagList": ["docker", "linux"],
  "runUntagged": true,
  "accessLevel": "not_protected",
  "jobsCount": 42,
  "contactedAt": 1609459200000,
  "version": "15.0.0",
  "revision": "abc123",
  "ipAddress": "192.168.1.100",
  "currentJobs": 2,
  "maxJobs": 4,
  "busy": true
}
```

**Параметры Runner:**
- **id** - Уникальный идентификатор (обязательно)
- **name** - Имя runner (обязательно)
- **description** - Описание runner (опционально)
- **status** - Статус: `online`, `offline`, `paused`, `not_connected`
- **runnerType** - Тип runner: `instance_type`, `group_type`, `project_type` (по умолчанию: `project_type`)
- **executor** - Executor: `docker`, `kubernetes`, `shell`, `docker-ssh`, `ssh`, `parallels`, `virtualbox`, `docker+machine`, `docker-ssh+machine`, `custom` (по умолчанию: `docker`)
- **platform** - Платформа: `linux`, `windows`, `osx` (опционально)
- **architecture** - Архитектура: `amd64`, `arm64`, etc. (опционально)
- **active** - Активен ли runner (по умолчанию: `true`)
- **isShared** - Является ли runner shared (по умолчанию: `false`)
- **locked** - Заблокирован ли runner (по умолчанию: `false`)
- **maximumTimeout** - Максимальный timeout для jobs в секундах (опционально)
- **tagList** - Теги для выбора jobs (массив строк, опционально)
- **runUntagged** - Выполнять ли untagged jobs (по умолчанию: `true`)
- **accessLevel** - Уровень доступа: `not_protected`, `ref_protected` (по умолчанию: `not_protected`)
- **jobsCount** - Общее количество выполненных jobs (опционально)
- **contactedAt** - Время последнего контакта (timestamp, опционально)
- **version** - Версия GitLab Runner (опционально)
- **revision** - Ревизия GitLab Runner (опционально)
- **ipAddress** - IP адрес runner (опционально)
- **currentJobs** - Текущее количество выполняемых jobs (runtime)
- **maxJobs** - Максимальное количество параллельных jobs (по умолчанию: `4`)
- **busy** - Занят ли runner (runtime, вычисляется автоматически)

**Статусы Runner:**
- **online** - Runner онлайн и доступен
- **offline** - Runner офлайн
- **paused** - Runner приостановлен
- **not_connected** - Runner не подключен

**Executors:**
- **docker** - Docker executor (по умолчанию)
- **kubernetes** - Kubernetes executor
- **shell** - Shell executor
- **docker-ssh** - Docker SSH executor
- **ssh** - SSH executor
- **parallels** - Parallels executor
- **virtualbox** - VirtualBox executor
- **docker+machine** - Docker Machine executor
- **docker-ssh+machine** - Docker SSH Machine executor
- **custom** - Custom executor

**Runner Tags:**
- Tags используются для выбора runner для jobs
- Job с тегами будет выполняться только на runner с соответствующими тегами
- Если `runUntagged: true`, runner может выполнять jobs без тегов

**Runner Selection:**
- Job выбирает runner на основе тегов
- Если job имеет теги, выбирается runner с соответствующими тегами
- Если job не имеет тегов, выбирается runner с `runUntagged: true`
- Выбирается runner с наименьшей загрузкой (currentJobs / maxJobs)

### 5. Artifact Management (Управление артефактами)

**Описание:** GitLab CI управляет артефактами, созданными jobs.

**Структура Artifact:**
```json
{
  "id": "artifact-123",
  "jobId": "job-123",
  "pipelineId": "pipeline-execution-123",
  "name": "dist",
  "size": 5242880,
  "fileType": "archive",
  "filename": "dist.zip",
  "createdAt": 1609459200000,
  "expiresAt": 1609545600000
}
```

**Параметры Artifact:**
- **id** - Уникальный идентификатор (генерируется автоматически)
- **jobId** - ID job, создавшего артефакт (обязательно)
- **pipelineId** - ID pipeline execution (обязательно)
- **name** - Имя артефакта (обязательно)
- **size** - Размер в байтах (обязательно)
- **fileType** - Тип файла: `archive`, `file`, `directory` (опционально)
- **filename** - Имя файла (опционально)
- **createdAt** - Время создания (timestamp, обязательно)
- **expiresAt** - Время истечения (timestamp, опционально)

**Artifact Expiry:**
- Артефакты могут иметь expiry время
- Expiry настраивается через `artifactsExpiry` в конфигурации (например, `7d`, `30d`)
- После expiry артефакты автоматически удаляются

**Artifact Dependencies:**
- Jobs могут зависеть от артефактов других jobs
- Зависимости указываются через `dependencies` в конфигурации job
- Артефакты загружаются перед выполнением job

**Artifact Types:**
- **Archive** - Архив (ZIP, TAR, etc.)
- **File** - Отдельный файл
- **Directory** - Директория

**Artifact Sizes:**
- Размеры артефактов генерируются реалистично на основе типа
- JAR файлы: 5-50 MB
- WAR файлы: 10-100 MB
- npm packages: 1-20 MB
- Docker images: 50-500 MB
- Build artifacts: 1-100 MB

### 6. Cache Management (Управление кэшем)

**Описание:** GitLab CI поддерживает кэширование для ускорения builds.

**Структура Cache:**
```json
{
  "key": "npm-cache-$CI_COMMIT_REF_SLUG",
  "paths": ["node_modules/"],
  "policy": "pull-push",
  "when": "on_success",
  "untracked": false
}
```

**Параметры Cache:**
- **key** - Ключ кэша (строка или объект с files/prefix, обязательно)
- **paths** - Пути для кэширования (массив строк, обязательно)
- **policy** - Политика кэша: `pull-push`, `pull`, `push` (по умолчанию: `pull-push`)
- **when** - Условие кэширования: `on_success`, `on_failure`, `always` (опционально)
- **untracked** - Кэшировать ли untracked файлы (по умолчанию: `false`)

**Cache Policies:**
- **pull-push** - Загружать кэш перед job, сохранять после job (по умолчанию)
- **pull** - Только загружать кэш перед job
- **push** - Только сохранять кэш после job

**Cache Keys:**
- Ключ кэша может быть строкой или объектом
- Строковый ключ: `"npm-cache-$CI_COMMIT_REF_SLUG"`
- Объектный ключ: `{ "files": ["package-lock.json"], "prefix": "npm" }`
- Объектный ключ создает ключ на основе содержимого файлов и prefix

**Cache Hit/Miss:**
- Cache hit происходит, если кэш с таким ключом существует
- Cache miss происходит, если кэш не найден
- Метрики cache hit rate рассчитываются автоматически

**Cache Types:**
- **s3** - S3 storage (по умолчанию)
- **gcs** - Google Cloud Storage
- **local** - Локальное хранилище

### 7. Variable Management (Управление переменными)

**Описание:** GitLab CI поддерживает CI/CD переменные для настройки jobs и pipelines.

**Структура Variable:**
```json
{
  "key": "NODE_ENV",
  "value": "production",
  "variableType": "env_var",
  "protected": false,
  "masked": false,
  "raw": false,
  "environmentScope": "*"
}
```

**Параметры Variable:**
- **key** - Ключ переменной (обязательно)
- **value** - Значение переменной (обязательно)
- **variableType** - Тип переменной: `env_var`, `file` (по умолчанию: `env_var`)
- **protected** - Защищенная переменная (только для protected branches/tags, по умолчанию: `false`)
- **masked** - Маскированная переменная (скрыта в логах, по умолчанию: `false`)
- **raw** - Raw переменная (не экранируется, по умолчанию: `false`)
- **environmentScope** - Scope окружения: `*` для всех, или конкретное окружение (по умолчанию: `*`)

**Типы переменных:**
- **env_var** - Environment variable (доступна как переменная окружения)
- **file** - File variable (сохраняется в файл, путь доступен через переменную)

**Protected Variables:**
- Protected переменные доступны только для protected branches/tags
- Используются для секретов, которые не должны быть доступны для всех веток

**Masked Variables:**
- Masked переменные скрыты в логах (заменяются на `[MASKED]`)
- Используются для паролей, токенов и других секретов

**Environment Scope:**
- Environment scope определяет, для каких окружений доступна переменная
- `*` - для всех окружений
- Конкретное окружение - только для указанного окружения

**Встроенные переменные:**
- GitLab CI предоставляет множество встроенных переменных
- Например: `CI_COMMIT_REF_NAME`, `CI_COMMIT_SHA`, `CI_PIPELINE_ID`, `CI_JOB_ID`, etc.
- Встроенные переменные доступны во всех jobs

### 8. Environment Management (Управление окружениями)

**Описание:** GitLab CI управляет окружениями для развертывания.

**Структура Environment:**
```json
{
  "id": "env-1",
  "name": "production",
  "slug": "production",
  "externalUrl": "https://app.example.com",
  "state": "available",
  "deployments": [
    {
      "id": "deployment-1",
      "status": "success",
      "createdAt": 1609459200000,
      "finishedAt": 1609459260000,
      "user": {
        "id": "user-1",
        "name": "John Doe",
        "username": "johndoe"
      }
    }
  ]
}
```

**Параметры Environment:**
- **id** - Уникальный идентификатор (обязательно)
- **name** - Имя окружения (обязательно)
- **slug** - Slug окружения (генерируется автоматически из name)
- **externalUrl** - Внешний URL окружения (опционально)
- **state** - Состояние: `available`, `stopped` (по умолчанию: `available`)
- **deployments** - Массив deployments (опционально)

**Статусы Environment:**
- **available** - Окружение доступно
- **stopped** - Окружение остановлено

**Deployments:**
- Deployments отслеживают развертывания в окружения
- Каждый deployment имеет статус, время создания и завершения
- Deployments создаются автоматически при выполнении jobs с `environment`

### 9. Schedule Management (Управление расписаниями)

**Описание:** GitLab CI поддерживает scheduled pipelines для автоматического запуска по расписанию.

**Структура Schedule:**
```json
{
  "id": "schedule-1",
  "description": "Nightly build",
  "ref": "main",
  "cron": "0 2 * * *",
  "cronTimezone": "UTC",
  "active": true,
  "nextRunAt": 1609545600000,
  "lastRunPipelineId": "pipeline-execution-123",
  "variables": {
    "DEPLOY_ENV": "production"
  }
}
```

**Параметры Schedule:**
- **id** - Уникальный идентификатор (обязательно)
- **description** - Описание schedule (обязательно)
- **ref** - Ветка или тег (обязательно)
- **cron** - Cron выражение (обязательно)
- **cronTimezone** - Timezone для cron (опционально, по умолчанию: `UTC`)
- **active** - Активен ли schedule (по умолчанию: `true`)
- **nextRunAt** - Время следующего запуска (timestamp, вычисляется автоматически)
- **lastRunPipelineId** - ID последнего запущенного pipeline (опционально)
- **variables** - CI/CD переменные для schedule (опционально)

**Cron синтаксис:**
- 5 полей: `minute hour day month weekday`
- Специальные символы:
  - `*` - любое значение
  - `,` - список значений (например: `1,15,30`)
  - `-` - диапазон (например: `1-5`)
  - `/` - шаг (например: `*/15` - каждые 15 минут)
  - `?` - любое значение (только для day/month)

**Примеры cron:**
- `0 2 * * *` - Каждый день в 2:00
- `0 */2 * * *` - Каждые 2 часа
- `0 0 * * 0` - Каждое воскресенье в полночь
- `0 0 1 * *` - Первое число каждого месяца в полночь
- `*/15 * * * *` - Каждые 15 минут

**Scheduled Pipelines:**
- Scheduled pipelines создаются автоматически по расписанию
- Источник pipeline: `schedule`
- Variables из schedule передаются в pipeline

### 10. YAML Import (Импорт YAML)

**Описание:** GitLab CI поддерживает импорт и парсинг .gitlab-ci.yml файлов.

**Поддерживаемые функции YAML:**
- **stages** - Определение stages
- **jobs** - Определение jobs с script, image, tags, etc.
- **extends** - Наследование конфигурации jobs
- **include** - Включение внешних файлов (в симуляции пропускается)
- **variables** - Глобальные переменные
- **cache** - Глобальный кэш
- **rules** - Правила для условного выполнения
- **needs** - Зависимости между jobs
- **artifacts** - Артефакты
- **dependencies** - Зависимости артефактов
- **when** - Условия выполнения
- **allow_failure** - Разрешение failure
- **retry** - Retry настройки
- **tags** - Теги для runners
- **image** - Docker image
- **services** - Docker services
- **before_script** - Команды перед script
- **after_script** - Команды после script

**Пример .gitlab-ci.yml:**
```yaml
stages:
  - build
  - test
  - deploy

variables:
  NODE_ENV: production

cache:
  key: npm-cache-$CI_COMMIT_REF_SLUG
  paths:
    - node_modules/

build-job:
  stage: build
  image: node:18
  tags:
    - docker
    - linux
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 7 days

test-job:
  stage: test
  image: node:18
  tags:
    - docker
    - linux
  script:
    - npm test
  needs:
    - build-job
  coverage: '/Coverage: \d+\.\d+%/'

deploy-job:
  stage: deploy
  image: node:18
  tags:
    - docker
    - linux
  script:
    - npm run deploy
  environment:
    name: production
    url: https://app.example.com
  when: manual
  only:
    - main
```

**Парсинг YAML:**
- YAML парсится с помощью js-yaml
- Поддерживается extends для наследования конфигурации
- Include пропускается в симуляции (внешние файлы не загружаются)
- Ошибки парсинга отображаются в UI

**Валидация YAML:**
- YAML валидируется перед импортом
- Проверяется синтаксис и структура
- Ошибки валидации отображаются пользователю

---

## Руководство пользователя

### Быстрый старт

1. **Добавьте компонент GitLab CI на канвас:**
   - Перетащите компонент "GitLab CI/CD" из библиотеки компонентов на канвас
   - Компонент будет создан с дефолтной конфигурацией

2. **Настройте базовые параметры:**
   - Откройте конфигурацию компонента
   - Настройте `gitlabUrl` (по умолчанию: `https://gitlab.com`)
   - Настройте `runnerType` (по умолчанию: `docker`)
   - Настройте `concurrentJobs` (по умолчанию: `4`)

3. **Добавьте runners:**
   - Перейдите на вкладку "Runners"
   - Нажмите "Add Runner"
   - Заполните имя, executor, maxJobs, tags
   - Сохраните runner

4. **Создайте pipeline:**
   - Перейдите на вкладку "Pipelines"
   - Нажмите "Add Pipeline" или "Import YAML"
   - Заполните конфигурацию pipeline или импортируйте .gitlab-ci.yml
   - Сохраните pipeline

5. **Запустите симуляцию:**
   - Нажмите кнопку "Start" в toolbar
   - Pipelines начнут выполняться автоматически
   - Наблюдайте за выполнением в реальном времени

### Работа с Pipelines

#### Создание Pipeline

1. **Через UI:**
   - Откройте вкладку "Pipelines"
   - Нажмите "Add Pipeline"
   - Заполните конфигурацию:
     - ID (уникальный идентификатор)
     - Ref (ветка или тег)
     - Source (источник pipeline)
     - Stages с jobs
   - Сохраните pipeline

2. **Через YAML Import:**
   - Откройте вкладку "Pipelines"
   - Нажмите "Import YAML"
   - Вставьте содержимое .gitlab-ci.yml файла
   - Нажмите "Import"
   - Pipeline будет создан на основе YAML

#### Запуск Pipeline

1. **Автоматический запуск:**
   - Pipelines с source `push` запускаются автоматически при симуляции
   - Pipelines с source `schedule` запускаются по расписанию
   - Pipelines с source `trigger` запускаются при получении trigger

2. **Ручной запуск:**
   - Выберите pipeline в списке
   - Нажмите кнопку "Play" (▶)
   - Pipeline будет запущен немедленно

3. **Retry Pipeline:**
   - Выберите failed pipeline
   - Нажмите кнопку "Retry" (↻)
   - Создается новый execution с тем же IID

#### Просмотр Pipeline

1. **Список Pipelines:**
   - Откройте вкладку "Pipelines"
   - Список показывает все pipeline executions
   - Фильтры по статусу, ref, source
   - Поиск по ID, ref, source

2. **Детали Pipeline:**
   - Кликните на pipeline в списке
   - Откроется детальная информация:
     - Статус и прогресс
     - Stages и jobs
     - Длительность и метрики
     - Variables и конфигурация

3. **Визуализация Pipeline:**
   - Нажмите кнопку "Visualize" для pipeline
   - Откроется визуализация графа stages и jobs
   - Показываются зависимости (needs)

### Работа с Jobs

#### Просмотр Jobs

1. **Список Jobs:**
   - Откройте вкладку "Jobs"
   - Список показывает все активные jobs
   - Фильтры по статусу, stage, runner
   - Сортировка по created, duration, status, name

2. **Детали Job:**
   - Кликните на job в списке
   - Откроется детальная информация:
     - Статус и прогресс
     - Логи выполнения
     - Артефакты
     - Variables и конфигурация
     - Runner и метрики

3. **Логи Job:**
   - В деталях job откройте вкладку "Logs"
   - Логи обновляются в реальном времени
   - Поиск по логам
   - Копирование логов

#### Управление Jobs

1. **Запуск Manual Job:**
   - Выберите manual job
   - Нажмите кнопку "Play" (▶)
   - Job будет запущен немедленно

2. **Отмена Job:**
   - Выберите running job
   - Нажмите кнопку "Cancel" (✕)
   - Job будет отменен

3. **Retry Job:**
   - Выберите failed job
   - Нажмите кнопку "Retry" (↻)
   - Job будет перезапущен

### Работа с Runners

#### Добавление Runner

1. **Через UI:**
   - Откройте вкладку "Runners"
   - Нажмите "Add Runner"
   - Заполните конфигурацию:
     - Name (имя runner)
     - Executor (docker, kubernetes, shell)
     - Max Jobs (максимальное количество параллельных jobs)
     - Tags (теги для выбора jobs)
     - Is Shared (shared runner или project runner)
   - Сохраните runner

2. **Через конфигурацию:**
   - Добавьте runner в конфигурацию компонента:
   ```json
   {
     "runners": [
       {
         "id": "runner-1",
         "name": "docker-runner-1",
         "executor": "docker",
         "maxJobs": 4,
         "tags": ["docker", "linux"],
         "isShared": false
       }
     ]
   }
   ```

#### Управление Runners

1. **Просмотр Runners:**
   - Откройте вкладку "Runners"
   - Список показывает все runners
   - Статус, executor, текущая загрузка
   - Метрики использования

2. **Редактирование Runner:**
   - Выберите runner в списке
   - Нажмите кнопку "Edit" (✎)
   - Измените конфигурацию
   - Сохраните изменения

3. **Удаление Runner:**
   - Выберите runner в списке
   - Нажмите кнопку "Delete" (🗑)
   - Подтвердите удаление

4. **Pause/Resume Runner:**
   - Выберите runner в списке
   - Нажмите кнопку "Pause" / "Resume"
   - Runner будет приостановлен/возобновлен

### Работа с Variables

#### Добавление Variable

1. **Через UI:**
   - Откройте вкладку "Variables"
   - Нажмите "Add Variable"
   - Заполните конфигурацию:
     - Key (ключ переменной)
     - Value (значение переменной)
     - Variable Type (env_var или file)
     - Protected (защищенная переменная)
     - Masked (маскированная переменная)
     - Environment Scope (scope окружения)
   - Сохраните variable

2. **Через конфигурацию:**
   - Добавьте variable в конфигурацию компонента:
   ```json
   {
     "variables": [
       {
         "key": "NODE_ENV",
         "value": "production",
         "protected": false,
         "masked": false,
         "environmentScope": "*"
       }
     ]
   }
   ```

#### Управление Variables

1. **Просмотр Variables:**
   - Откройте вкладку "Variables"
   - Список показывает все variables
   - Тип, protected, masked, scope
   - Значения (masked переменные скрыты)

2. **Редактирование Variable:**
   - Выберите variable в списке
   - Нажмите кнопку "Edit" (✎)
   - Измените конфигурацию
   - Сохраните изменения

3. **Удаление Variable:**
   - Выберите variable в списке
   - Нажмите кнопку "Delete" (🗑)
   - Подтвердите удаление

### Работа с Environments

#### Добавление Environment

1. **Через UI:**
   - Откройте вкладку "Environments"
   - Нажмите "Add Environment"
   - Заполните конфигурацию:
     - ID (уникальный идентификатор)
     - Name (имя окружения)
     - External URL (внешний URL, опционально)
   - Сохраните environment

2. **Через конфигурацию:**
   - Добавьте environment в конфигурацию компонента:
   ```json
   {
     "environments": [
       {
         "id": "env-1",
         "name": "production",
         "externalUrl": "https://app.example.com"
       }
     ]
   }
   ```

#### Управление Environments

1. **Просмотр Environments:**
   - Откройте вкладку "Environments"
   - Список показывает все environments
   - Состояние, URL, deployments
   - Метрики использования

2. **Редактирование Environment:**
   - Выберите environment в списке
   - Нажмите кнопку "Edit" (✎)
   - Измените конфигурацию
   - Сохраните изменения

3. **Удаление Environment:**
   - Выберите environment в списке
   - Нажмите кнопку "Delete" (🗑)
   - Подтвердите удаление

### Работа с Schedules

#### Добавление Schedule

1. **Через UI:**
   - Откройте вкладку "Schedules"
   - Нажмите "Add Schedule"
   - Заполните конфигурацию:
     - ID (уникальный идентификатор)
     - Description (описание schedule)
     - Ref (ветка или тег)
     - Cron (cron выражение)
     - Active (активен ли schedule)
     - Variables (CI/CD переменные, опционально)
   - Сохраните schedule

2. **Через конфигурацию:**
   - Добавьте schedule в конфигурацию компонента:
   ```json
   {
     "schedules": [
       {
         "id": "schedule-1",
         "description": "Nightly build",
         "ref": "main",
         "cron": "0 2 * * *",
         "active": true,
         "variables": {
           "DEPLOY_ENV": "production"
         }
       }
     ]
   }
   ```

#### Управление Schedules

1. **Просмотр Schedules:**
   - Откройте вкладку "Schedules"
   - Список показывает все schedules
   - Cron, next run, last run, active status
   - Метрики использования

2. **Редактирование Schedule:**
   - Выберите schedule в списке
   - Нажмите кнопку "Edit" (✎)
   - Измените конфигурацию
   - Сохраните изменения

3. **Удаление Schedule:**
   - Выберите schedule в списке
   - Нажмите кнопку "Delete" (🗑)
   - Подтвердите удаление

4. **Активация/Деактивация Schedule:**
   - Выберите schedule в списке
   - Переключите "Active" switch
   - Schedule будет активирован/деактивирован

---

## Руководство администратора

### Рекомендации по конфигурации

#### Production конфигурация

**Рекомендуемая конфигурация для production:**

```json
{
  "gitlabUrl": "https://gitlab.example.com",
  "enableRunners": true,
  "runnerType": "kubernetes",
  "concurrentJobs": 8,
  "enableCache": true,
  "cacheType": "s3",
  "enableArtifacts": true,
  "artifactsExpiry": "30d",
  "enableKubernetes": true,
  "k8sNamespace": "gitlab-runner",
  "runners": [
    {
      "id": "runner-k8s-1",
      "name": "kubernetes-runner-1",
      "executor": "kubernetes",
      "maxJobs": 8,
      "tags": ["kubernetes", "production"],
      "isShared": true
    },
    {
      "id": "runner-k8s-2",
      "name": "kubernetes-runner-2",
      "executor": "kubernetes",
      "maxJobs": 8,
      "tags": ["kubernetes", "production"],
      "isShared": true
    }
  ],
  "variables": [
    {
      "key": "DEPLOY_ENV",
      "value": "production",
      "protected": true,
      "masked": false,
      "environmentScope": "production"
    }
  ]
}
```

**Ключевые параметры:**
- **runnerType**: `kubernetes` для масштабируемости
- **concurrentJobs**: 8 для высокой производительности
- **cacheType**: `s3` для распределенного кэша
- **artifactsExpiry**: `30d` для долгого хранения артефактов
- **runners**: Несколько shared runners для высокой доступности
- **variables**: Protected variables для production окружений

#### Development конфигурация

**Рекомендуемая конфигурация для development:**

```json
{
  "gitlabUrl": "https://gitlab.example.com",
  "enableRunners": true,
  "runnerType": "docker",
  "concurrentJobs": 4,
  "enableCache": true,
  "cacheType": "local",
  "enableArtifacts": true,
  "artifactsExpiry": "7d",
  "runners": [
    {
      "id": "runner-docker-1",
      "name": "docker-runner-1",
      "executor": "docker",
      "maxJobs": 4,
      "tags": ["docker", "development"],
      "isShared": false
    }
  ]
}
```

**Ключевые параметры:**
- **runnerType**: `docker` для простоты
- **concurrentJobs**: 4 для умеренной нагрузки
- **cacheType**: `local` для локального кэша
- **artifactsExpiry**: `7d` для короткого хранения артефактов
- **runners**: Один project runner для development

### Оптимизация производительности

#### Настройка Runners

1. **Количество Runners:**
   - Для высокой нагрузки: 3-5 shared runners
   - Для умеренной нагрузки: 1-2 runners
   - Для низкой нагрузки: 1 runner

2. **Max Jobs per Runner:**
   - Для мощных runners: 8-16 jobs
   - Для средних runners: 4-8 jobs
   - Для слабых runners: 2-4 jobs

3. **Runner Tags:**
   - Используйте теги для распределения нагрузки
   - Например: `docker`, `kubernetes`, `linux`, `windows`
   - Jobs с тегами будут выполняться на соответствующих runners

#### Настройка Cache

1. **Cache Policy:**
   - `pull-push` для большинства случаев (по умолчанию)
   - `pull` для jobs, которые только читают кэш
   - `push` для jobs, которые только пишут кэш

2. **Cache Keys:**
   - Используйте специфичные ключи для разных веток
   - Например: `npm-cache-$CI_COMMIT_REF_SLUG`
   - Используйте files-based keys для автоматической инвалидации

3. **Cache Paths:**
   - Кэшируйте только необходимые директории
   - Например: `node_modules/`, `vendor/`, `.gradle/`
   - Избегайте кэширования больших директорий

#### Настройка Artifacts

1. **Artifact Expiry:**
   - Production: `30d` для долгого хранения
   - Development: `7d` для короткого хранения
   - CI: `1d` для временного хранения

2. **Artifact Paths:**
   - Сохраняйте только необходимые артефакты
   - Используйте wildcards для фильтрации
   - Например: `dist/**/*`, `target/*.jar`

#### Оптимизация Pipelines

1. **Использование Needs:**
   - Используйте `needs` для параллельного выполнения jobs
   - Уменьшает общее время выполнения pipeline
   - Пример:
   ```yaml
   test-job:
     needs:
       - build-job
   ```

2. **Использование Rules:**
   - Используйте `rules` для условного выполнения jobs
   - Пропускайте ненужные jobs для экономии ресурсов
   - Пример:
   ```yaml
   deploy-job:
     rules:
       - if: $CI_COMMIT_BRANCH == 'main'
   ```

3. **Использование Parallel Jobs:**
   - Выполняйте независимые jobs параллельно
   - Используйте несколько runners для параллельного выполнения
   - Уменьшает общее время выполнения pipeline

### Безопасность

#### Protected Variables

1. **Использование Protected Variables:**
   - Используйте protected variables для секретов
   - Protected variables доступны только для protected branches/tags
   - Пример:
   ```json
   {
     "key": "API_KEY",
     "value": "secret-key",
     "protected": true
   }
   ```

2. **Masked Variables:**
   - Используйте masked variables для паролей и токенов
   - Masked variables скрыты в логах
   - Пример:
   ```json
   {
     "key": "PASSWORD",
     "value": "secret-password",
     "masked": true
   }
   ```

#### Runner Security

1. **Runner Tags:**
   - Используйте теги для изоляции jobs
   - Разделяйте production и development runners
   - Пример: `production`, `development`, `staging`

2. **Runner Access:**
   - Используйте project runners для изоляции
   - Shared runners доступны для всех проектов
   - Locked runners доступны только для определенных проектов

#### Pipeline Security

1. **Pipeline Sources:**
   - Ограничьте pipeline sources для безопасности
   - Используйте только необходимые sources
   - Пример: только `push` и `schedule` для production

2. **Job Permissions:**
   - Используйте `allowFailure: false` для критичных jobs
   - Используйте `when: manual` для опасных операций
   - Пример: deployment jobs должны быть manual

### Мониторинг и алертинг

#### Метрики для мониторинга

1. **Pipeline Metrics:**
   - `pipelinesPerHour` - Количество pipelines в час
   - `averagePipelineDuration` - Средняя длительность pipeline
   - `pipelinesFailed` - Количество failed pipelines
   - `pipelinesSuccess` - Количество successful pipelines

2. **Job Metrics:**
   - `jobsRunning` - Текущее количество running jobs
   - `jobsPending` - Текущее количество pending jobs
   - `averageJobDuration` - Средняя длительность job
   - `jobsFailed` - Количество failed jobs

3. **Runner Metrics:**
   - `runnerUtilization` - Использование runners (0-100%)
   - `runnersOnline` - Количество online runners
   - `runnersBusy` - Количество busy runners
   - `runnersIdle` - Количество idle runners

4. **Cache Metrics:**
   - `cacheHitRate` - Cache hit rate (0-1)
   - `cacheHits` - Количество cache hits
   - `cacheMisses` - Количество cache misses

#### Интеграция с Prometheus

1. **Экспорт метрик:**
   - GitLab CI автоматически экспортирует метрики в Prometheus format
   - Метрики доступны через `exportPrometheusMetrics()`
   - Подключите Prometheus для scraping метрик

2. **Пример конфигурации Prometheus:**
   ```yaml
   scrape_configs:
     - job_name: 'gitlab-ci'
       static_configs:
         - targets: ['gitlab-ci:9090']
   ```

#### Интеграция с PagerDuty

1. **Отправка алертов:**
   - Настройте `resultDestinations` для отправки алертов
   - Используйте `condition: on_failure` для failed pipelines
   - Пример:
   ```json
   {
     "resultDestinations": [
       {
         "targetComponentId": "pagerduty-1",
         "format": "pagerduty",
         "condition": "on_failure"
       }
     ]
   }
   ```

#### Интеграция с Loki

1. **Экспорт логов:**
   - GitLab CI автоматически экспортирует логи jobs в Loki
   - Логи доступны через `exportLogsToLoki()`
   - Подключите Loki для централизованного логирования

---

## Метрики и мониторинг

### Метрики компонента

GitLab CI предоставляет следующие метрики компонента (через `ComponentMetrics`):

- **throughput** - Количество pipelines в секунду (pipelinesPerHour / 3600)
- **latency** - Средняя длительность pipeline в миллисекундах
- **utilization** - Использование runners (0-1, runnerUtilization / 100)
- **errorRate** - Процент failed pipelines (0-1, pipelinesFailed / totalPipelines)

### Метрики GitLab CI

GitLab CI предоставляет следующие специфичные метрики (через `GitLabCIEngineMetrics`):

#### Pipeline Metrics

- **pipelinesTotal** - Общее количество pipelines (counter)
- **pipelinesSuccess** - Количество successful pipelines (counter)
- **pipelinesFailed** - Количество failed pipelines (counter)
- **pipelinesRunning** - Текущее количество running pipelines (gauge)
- **pipelinesPending** - Текущее количество pending pipelines (gauge)
- **pipelinesPerHour** - Количество pipelines в час (gauge)
- **averagePipelineDuration** - Средняя длительность pipeline в миллисекундах (gauge)

#### Job Metrics

- **jobsTotal** - Общее количество jobs (counter)
- **jobsSuccess** - Количество successful jobs (counter)
- **jobsFailed** - Количество failed jobs (counter)
- **jobsRunning** - Текущее количество running jobs (gauge)
- **jobsPending** - Текущее количество pending jobs (gauge)
- **averageJobDuration** - Средняя длительность job в миллисекундах (gauge)

#### Runner Metrics

- **runnersTotal** - Общее количество runners (gauge)
- **runnersOnline** - Количество online runners (gauge)
- **runnersBusy** - Количество busy runners (gauge)
- **runnersIdle** - Количество idle runners (gauge)
- **runnerUtilization** - Использование runners в процентах (0-100, gauge)

#### Cache Metrics

- **cacheHits** - Количество cache hits (counter)
- **cacheMisses** - Количество cache misses (counter)
- **cacheHitRate** - Cache hit rate (0-1, gauge)

#### Artifact Metrics

- **artifactsTotal** - Общее количество artifacts (gauge)
- **artifactsSizeBytes** - Общий размер artifacts в байтах (gauge)

#### Request Metrics

- **requestsTotal** - Общее количество запросов (counter)
- **requestsErrors** - Количество ошибок запросов (counter)

#### Coverage Metrics

- **coverage** - Средний code coverage процент (gauge, опционально)

### Prometheus Export

GitLab CI экспортирует метрики в Prometheus format через `exportPrometheusMetrics()`. Метрики доступны в формате Prometheus exposition format:

```
# HELP gitlab_ci_pipelines_total Total number of pipelines
# TYPE gitlab_ci_pipelines_total counter
gitlab_ci_pipelines_total{component_id="gitlab-ci-1",component_type="gitlab-ci"} 42 1609459200000

# HELP gitlab_ci_pipelines_success Total number of successful pipelines
# TYPE gitlab_ci_pipelines_success counter
gitlab_ci_pipelines_success{component_id="gitlab-ci-1",component_type="gitlab-ci"} 40 1609459200000

...
```

### Loki Export

GitLab CI экспортирует логи jobs в Loki через `exportLogsToLoki()`. Логи доступны в формате Loki Push API:

```json
{
  "streams": [
    {
      "stream": {
        "component_id": "gitlab-ci-1",
        "job_id": "job-123",
        "pipeline_id": "pipeline-123",
        "job_name": "build-job",
        "stage": "build"
      },
      "values": [
        ["1609459200000000000", "$ npm install"],
        ["1609459201000000000", "added 1234 packages in 30s"]
      ]
    }
  ]
}
```

---

## Примеры использования

### Пример 1: Простой Pipeline

**Конфигурация:**
```json
{
  "pipelines": [
    {
      "id": "simple-pipeline",
      "ref": "main",
      "source": "push",
      "stages": [
        {
          "name": "build",
          "jobs": [
            {
              "name": "build-job",
              "stage": "build",
              "script": ["npm install", "npm run build"],
              "image": "node:18",
              "tags": ["docker"]
            }
          ]
        }
      ]
    }
  ]
}
```

**Описание:**
- Простой pipeline с одним stage и одним job
- Job выполняет установку зависимостей и сборку
- Использует Docker image node:18

### Пример 2: Pipeline с несколькими Stages

**Конфигурация:**
```json
{
  "pipelines": [
    {
      "id": "multi-stage-pipeline",
      "ref": "main",
      "source": "push",
      "stages": [
        {
          "name": "build",
          "jobs": [
            {
              "name": "build-job",
              "stage": "build",
              "script": ["npm install", "npm run build"],
              "image": "node:18",
              "tags": ["docker"],
              "artifacts": ["dist/"]
            }
          ]
        },
        {
          "name": "test",
          "jobs": [
            {
              "name": "test-job",
              "stage": "test",
              "script": ["npm test"],
              "image": "node:18",
              "tags": ["docker"],
              "needs": ["build-job"],
              "dependencies": ["build-job"]
            }
          ]
        },
        {
          "name": "deploy",
          "jobs": [
            {
              "name": "deploy-job",
              "stage": "deploy",
              "script": ["npm run deploy"],
              "image": "node:18",
              "tags": ["docker"],
              "needs": ["test-job"],
              "when": "manual",
              "environment": "production"
            }
          ]
        }
      ]
    }
  ]
}
```

**Описание:**
- Pipeline с тремя stages: build, test, deploy
- Test job зависит от build job (needs)
- Deploy job является manual и требует ручного запуска
- Использует artifacts для передачи данных между jobs

### Пример 3: Pipeline с Rules

**Конфигурация:**
```json
{
  "pipelines": [
    {
      "id": "rules-pipeline",
      "ref": "main",
      "source": "push",
      "stages": [
        {
          "name": "build",
          "jobs": [
            {
              "name": "build-job",
              "stage": "build",
              "script": ["npm install", "npm run build"],
              "image": "node:18",
              "tags": ["docker"],
              "rules": [
                {
                  "if": "$CI_COMMIT_BRANCH == 'main'",
                  "when": "on_success"
                },
                {
                  "if": "$CI_COMMIT_BRANCH == 'develop'",
                  "when": "on_success"
                }
              ]
            }
          ]
        },
        {
          "name": "deploy",
          "jobs": [
            {
              "name": "deploy-production",
              "stage": "deploy",
              "script": ["npm run deploy:production"],
              "image": "node:18",
              "tags": ["docker"],
              "rules": [
                {
                  "if": "$CI_COMMIT_BRANCH == 'main'",
                  "when": "manual"
                }
              ]
            },
            {
              "name": "deploy-staging",
              "stage": "deploy",
              "script": ["npm run deploy:staging"],
              "image": "node:18",
              "tags": ["docker"],
              "rules": [
                {
                  "if": "$CI_COMMIT_BRANCH == 'develop'",
                  "when": "on_success"
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}
```

**Описание:**
- Pipeline с условным выполнением jobs на основе rules
- Build job выполняется для main и develop веток
- Deploy-production job выполняется только для main ветки (manual)
- Deploy-staging job выполняется только для develop ветки (automatic)

### Пример 4: Pipeline с Cache

**Конфигурация:**
```json
{
  "pipelines": [
    {
      "id": "cache-pipeline",
      "ref": "main",
      "source": "push",
      "stages": [
        {
          "name": "build",
          "jobs": [
            {
              "name": "build-job",
              "stage": "build",
              "script": ["npm install", "npm run build"],
              "image": "node:18",
              "tags": ["docker"],
              "cache": {
                "key": "npm-cache-$CI_COMMIT_REF_SLUG",
                "paths": ["node_modules/"],
                "policy": "pull-push",
                "when": "on_success"
              }
            }
          ]
        }
      ]
    }
  ]
}
```

**Описание:**
- Pipeline с кэшированием node_modules
- Cache key основан на ветке (CI_COMMIT_REF_SLUG)
- Cache policy: pull-push (загружает и сохраняет кэш)
- Cache сохраняется только при успешном выполнении job

### Пример 5: Pipeline с интеграциями

**Конфигурация:**
```json
{
  "pipelines": [
    {
      "id": "integration-pipeline",
      "ref": "main",
      "source": "push",
      "stages": [
        {
          "name": "build",
          "jobs": [
            {
              "name": "build-job",
              "stage": "build",
              "script": ["npm install", "npm run build"],
              "image": "node:18",
              "tags": ["docker"]
            }
          ]
        },
        {
          "name": "deploy",
          "jobs": [
            {
              "name": "deploy-k8s",
              "stage": "deploy",
              "script": ["kubectl apply -f k8s/"],
              "image": "node:18",
              "tags": ["kubernetes"],
              "integrations": {
                "deploy": {
                  "targetComponentId": "kubernetes-1",
                  "deployType": "deployment",
                  "manifests": [
                    {
                      "apiVersion": "apps/v1",
                      "kind": "Deployment",
                      "metadata": {
                        "name": "my-app"
                      }
                    }
                  ]
                }
              }
            }
          ]
        }
      ],
      "resultDestinations": [
        {
          "targetComponentId": "prometheus-1",
          "format": "prometheus",
          "condition": "always"
        },
        {
          "targetComponentId": "pagerduty-1",
          "format": "pagerduty",
          "condition": "on_failure"
        }
      ]
    }
  ]
}
```

**Описание:**
- Pipeline с интеграциями с другими компонентами
- Deploy job развертывает в Kubernetes
- Результаты pipeline отправляются в Prometheus (всегда)
- Алерты отправляются в PagerDuty (при failure)

---

## FAQ

### Как создать pipeline?

Вы можете создать pipeline двумя способами:
1. **Через UI:** Откройте вкладку "Pipelines", нажмите "Add Pipeline", заполните конфигурацию
2. **Через YAML Import:** Откройте вкладку "Pipelines", нажмите "Import YAML", вставьте содержимое .gitlab-ci.yml

### Как запустить pipeline вручную?

Выберите pipeline в списке и нажмите кнопку "Play" (▶). Pipeline будет запущен немедленно.

### Как сделать retry pipeline?

Выберите failed pipeline и нажмите кнопку "Retry" (↻). Создается новый execution с тем же IID.

### Как добавить runner?

Откройте вкладку "Runners", нажмите "Add Runner", заполните конфигурацию (имя, executor, maxJobs, tags), сохраните runner.

### Как использовать cache?

Добавьте конфигурацию cache в job:
```json
{
  "cache": {
    "key": "npm-cache-$CI_COMMIT_REF_SLUG",
    "paths": ["node_modules/"],
    "policy": "pull-push"
  }
}
```

### Как использовать needs для зависимостей?

Добавьте `needs` в конфигурацию job:
```json
{
  "name": "test-job",
  "needs": ["build-job"]
}
```

### Как использовать rules для условного выполнения?

Добавьте `rules` в конфигурацию job:
```json
{
  "name": "deploy-job",
  "rules": [
    {
      "if": "$CI_COMMIT_BRANCH == 'main'",
      "when": "manual"
    }
  ]
}
```

### Как импортировать .gitlab-ci.yml?

Откройте вкладку "Pipelines", нажмите "Import YAML", вставьте содержимое .gitlab-ci.yml файла, нажмите "Import".

### Как настроить интеграции с другими компонентами?

Добавьте `integrations` в конфигурацию job или `resultDestinations` в конфигурацию pipeline:
```json
{
  "integrations": {
    "deploy": {
      "targetComponentId": "kubernetes-1",
      "deployType": "deployment"
    }
  }
}
```

### Как экспортировать метрики в Prometheus?

GitLab CI автоматически экспортирует метрики в Prometheus format. Подключите Prometheus для scraping метрик через `exportPrometheusMetrics()`.

### Как экспортировать логи в Loki?

GitLab CI автоматически экспортирует логи jobs в Loki. Подключите Loki для централизованного логирования через `exportLogsToLoki()`.

---

## Дополнительные ресурсы

- [Официальная документация GitLab CI/CD](https://docs.gitlab.com/ee/ci/)
- [GitLab CI/CD YAML Reference](https://docs.gitlab.com/ee/ci/yaml/)
- [GitLab Runner Documentation](https://docs.gitlab.com/runner/)
- [GitLab CI/CD Best Practices](https://docs.gitlab.com/ee/ci/pipelines/pipeline_efficiency.html)
- [GitLab CI/CD Variables](https://docs.gitlab.com/ee/ci/variables/)
- [GitLab CI/CD Environments](https://docs.gitlab.com/ee/ci/environments/)
- [GitLab CI/CD Schedules](https://docs.gitlab.com/ee/ci/pipelines/schedules.html)
- [GitLab CI/CD Cache](https://docs.gitlab.com/ee/ci/caching/)
- [GitLab CI/CD Artifacts](https://docs.gitlab.com/ee/ci/pipelines/job_artifacts.html)
