# Дополнительные функции из drawdb для интеграции

## Анализ возможностей drawdb

### ✅ Уже реализовано
1. ✅ Визуализация схемы БД (таблицы и связи)
2. ✅ Перетаскивание таблиц
3. ✅ Zoom, pan, fit to screen
4. ✅ Экспорт SVG
5. ✅ Автоматическое определение связей (foreign keys)
6. ✅ Улучшенная визуализация связей:
   - ✅ Кардинальность (1, n обозначения на концах связей)
   - ✅ Метки на связях (названия полей)
   - ✅ Визуальные индикаторы constraint actions (C, R, N, D badges)
   - ✅ Hover эффекты и интерактивность
7. ✅ Экспорт SQL (CREATE TABLE, ALTER TABLE, FOREIGN KEY)
8. ✅ Экспорт Mermaid (для документации)
9. ✅ Экспорт DBML (для dbdiagram.io)
10. ✅ Экспорт Markdown документации

### 🎯 Приоритетные функции для интеграции

#### 1. **Экспорт SQL** (Высокий приоритет) ✅ ЗАВЕРШЕНО
- Генерация CREATE TABLE из схемы
- Генерация ALTER TABLE для foreign keys
- Генерация индексов
- Генерация комментариев (COMMENT ON TABLE/COLUMN)
- Поддержка ENUM типов
- Поддержка custom types

**Файлы из drawdb:**
- `src/utils/exportSQL/postgres.js` - генерация PostgreSQL SQL
- `src/utils/exportSQL/shared.js` - общие утилиты

**Интеграция:**
- Добавить кнопку "Export SQL" в таб Schema Diagram
- Генерировать SQL на основе текущей схемы
- Показывать в модальном окне или текстовом редакторе
- Возможность копирования и сохранения

#### 2. **Импорт SQL** (Высокий приоритет) ✅ ЗАВЕРШЕНО
- ✅ Парсинг CREATE TABLE
- ✅ Автоматическое создание таблиц из SQL
- ✅ Парсинг foreign keys (ALTER TABLE и inline в CREATE TABLE)
- ⚠️ Парсинг индексов (базовая поддержка, можно улучшить)
- ⚠️ Парсинг комментариев (не реализовано, можно добавить)

**Файлы:**
- ✅ `src/utils/schemaImport.ts` - парсинг PostgreSQL SQL
- ✅ Интеграция в `PostgreSQLConfigAdvanced.tsx`

**Интеграция:**
- ✅ Добавлена кнопка "Import SQL" в таб Schema Diagram
- ✅ Модальное окно с текстовым редактором
- ✅ Парсинг SQL и автоматическое создание таблиц
- ✅ Валидация и обработка ошибок

#### 3. **Экспорт в Mermaid** (Средний приоритет) ✅ ЗАВЕРШЕНО
- Генерация Mermaid ER диаграммы
- Для использования в документации (GitHub, GitLab, etc.)

**Файлы из drawdb:**
- `src/utils/exportAs/mermaid.js`

**Интеграция:**
- Кнопка "Export Mermaid" в таб Schema Diagram
- Копирование в буфер обмена
- Показ в модальном окне

#### 4. **Экспорт в DBML** (Средний приоритет) ✅ ЗАВЕРШЕНО
- Генерация DBML формата
- Для использования в dbdiagram.io

**Файлы из drawdb:**
- `src/utils/exportAs/dbml.js`

**Интеграция:**
- Кнопка "Export DBML" в таб Schema Diagram
- Копирование в буфер обмена

#### 5. **Экспорт документации** (Средний приоритет) ✅ ЗАВЕРШЕНО
- ✅ Генерация Markdown документации
- ✅ Описание таблиц, полей, связей
- ✅ Встроенная Mermaid диаграмма

**Файлы:**
- ✅ `src/utils/schemaExport.ts` - функция `exportDocumentationSchema()`

**Интеграция:**
- ✅ Кнопка "Export Documentation" в таб Schema Diagram
- ✅ Скачивание как .md файл

#### 6. **Автоматическое расположение таблиц** (Средний приоритет) ✅ ЗАВЕРШЕНО
- ✅ Автоматическая расстановка таблиц на диаграмме
- ✅ Алгоритм размещения в сетке, force-directed, hierarchical

**Файлы:**
- ✅ `src/utils/schemaArrangement.ts` - реализовано

**Интеграция:**
- ✅ Кнопка "Auto Arrange" в SchemaDiagram компоненте
- ✅ Автоматическая расстановка всех таблиц с учетом связей

#### 7. **Экспорт изображений** (Низкий приоритет) ✅ ЗАВЕРШЕНО
- ✅ Экспорт в PNG
- ✅ Экспорт в JPEG
- ✅ Высокое разрешение для документации (pixelRatio: 2)

**Файлы:**
- ✅ Использует `html-to-image` библиотеку (установлено)
- ✅ Интегрировано в PostgreSQLConfigAdvanced

**Интеграция:**
- ✅ Опции экспорта: PNG, JPEG в dropdown меню Export
- ✅ Автоматическое скачивание файлов

#### 8. **Комментарии к таблицам и полям** (Средний приоритет)
- Добавление комментариев к таблицам
- Добавление комментариев к полям
- Отображение в диаграмме

**Интеграция:**
- Добавить поле "comment" в интерфейс редактирования таблиц
- Отображение комментариев в tooltip или под таблицей
- Экспорт комментариев в SQL (COMMENT ON)

#### 9. **Цветовая схема таблиц** (Низкий приоритет)
- Настройка цвета для каждой таблицы
- Визуальная группировка

**Интеграция:**
- Color picker в редакторе таблиц
- Сохранение цвета в конфиге

#### 10. **Улучшение визуализации связей** (Высокий приоритет) ✅ ЗАВЕРШЕНО
- Отображение кардинальности (1, n, 1-to-many, many-to-one)
- Метки на связях (название связи, constraint actions)
- Интерактивность (клик для редактирования, hover для информации)
- Визуальные индикаторы constraint actions (CASCADE, RESTRICT, SET NULL)
- Улучшенные стили для разных типов связей

**Файлы из drawdb:**
- `src/components/EditorCanvas/Relationship.jsx` - компонент связи с кардинальностью
- `src/data/constants.js` - Cardinality enum

**Интеграция:**
- Улучшить компонент relationships в SchemaDiagram
- Добавить отображение кардинальности на концах связей
- Добавить метки с названиями полей и constraint actions
- Добавить hover эффекты и клик для редактирования

#### 11. **Валидация схемы** (Средний приоритет) ✅ ЗАВЕРШЕНО
- ✅ Проверка корректности схемы
- ✅ Проверка foreign keys
- ✅ Проверка типов данных
- ✅ Проверка имен таблиц и колонок
- ✅ Проверка первичных ключей
- ✅ Проверка циклических зависимостей

**Файлы:**
- ✅ `src/utils/schemaValidation.ts` - реализовано

**Интеграция:**
- ✅ Кнопка "Validate Schema" в PostgreSQLConfigAdvanced
- ✅ Диалог с отображением ошибок и предупреждений
- ✅ Разделение на errors и warnings

## План реализации

### Этап 0: Улучшение визуализации связей (Приоритет 1) ✅ ЗАВЕРШЕНО
- [x] Базовая визуализация связей (SVG path с стрелками)
- [x] Отображение кардинальности (1, n обозначения на концах связей)
- [x] Метки на связях (название связи, constraint actions)
- [x] Визуальные индикаторы constraint actions (C, R, N, D badges)
- [x] Улучшенные стили для разных типов связей (hover эффекты)
- [x] Интерактивность (клик для информации - console.log, можно расширить до модального окна)

### Этап 1: Экспорт SQL (Приоритет 1) ✅ ЗАВЕРШЕНО
- [x] Создать `src/utils/schemaExport.ts` с функцией `exportPostgreSQLSchema()`
- [x] Адаптировать код из `drawdb/src/utils/exportSQL/postgres.js`
- [x] Реализовать экспорт в Mermaid
- [x] Реализовать экспорт в DBML
- [x] Реализовать экспорт документации
- [x] Добавить кнопку "Export" в SchemaDiagram
- [x] Модальное окно с SQL кодом и кнопкой копирования
- [x] Dropdown меню "Export" с опциями (SQL, Mermaid, DBML, Documentation)
- [x] Кнопка скачивания файла

### Этап 2: Импорт SQL (Приоритет 1) ✅ ЗАВЕРШЕНО
- [x] Создать `src/utils/schemaImport.ts` с функцией `importPostgreSQLSchema()`
- [x] Адаптировать код из `drawdb/src/utils/importSQL/postgres.js`
- [x] Добавить кнопку "Import SQL" в PostgreSQLConfigAdvanced (рядом с Export)
- [x] Модальное окно с текстовым редактором
- [x] Парсинг и создание таблиц
- [x] Парсинг ALTER TABLE для foreign keys
- [x] Парсинг inline FOREIGN KEY в CREATE TABLE
- [x] Обработка ошибок и валидация

### Этап 3: Экспорт в другие форматы (Приоритет 2) ✅ ЗАВЕРШЕНО
- [x] Экспорт Mermaid
- [x] Экспорт DBML
- [x] Экспорт документации
- [x] Добавить dropdown меню "Export" с опциями

### Этап 4: Дополнительные функции (Приоритет 3) ✅ ЗАВЕРШЕНО
- [x] Автоматическое расположение таблиц ✅
- [ ] Комментарии к таблицам и полям (частично - типы добавлены, UI в процессе)
- [x] Валидация схемы ✅
- [x] Экспорт изображений (PNG, JPEG) ✅

## Технические детали

### Зависимости
- `node-sql-parser` - уже есть в проекте ✅
- `html-to-image` - добавлено для экспорта изображений ✅

### Структура файлов
```
src/
  utils/
    schemaExport.ts       # Экспорт SQL, Mermaid, DBML, документации ✅
    schemaImport.ts      # Импорт SQL ✅
    schemaValidation.ts  # Валидация схемы ✅
    schemaArrangement.ts # Автоматическое расположение таблиц ✅
  components/
    config/
      data/
        SchemaDiagram.tsx          # Уже есть, обновлено с auto-arrange ✅
        PostgreSQLConfigAdvanced.tsx # Интегрированы все функции ✅
```

## Примеры использования

### Экспорт SQL
```typescript
const sql = exportPostgreSQLSchema(tables, relationships);
// Результат:
// CREATE TABLE "users" (
//   "id" SERIAL NOT NULL,
//   "username" VARCHAR(255) NOT NULL,
//   PRIMARY KEY("id")
// );
// 
// ALTER TABLE "orders"
// ADD FOREIGN KEY("user_id") REFERENCES "users"("id")
// ON UPDATE CASCADE ON DELETE RESTRICT;
```

### Импорт SQL
```typescript
const sql = `
  CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(255) NOT NULL
  );
  
  CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id)
  );
`;

const { tables, relationships } = importPostgreSQLSchema(sql);
// Автоматически создает таблицы и связи
```

### Экспорт Mermaid
```typescript
const mermaid = exportMermaidSchema(tables, relationships);
// Результат:
// erDiagram
//   users ||--o{ orders : references
//   
//   users {
//     SERIAL id
//     VARCHAR username
//   }
```

## Реализованные функции (Этап 4)

### ✅ Автоматическое расположение таблиц
**Файлы:**
- `src/utils/schemaArrangement.ts` - утилиты для автоматического расположения

**Методы расположения:**
1. **Grid** - расположение в сетке
2. **Force-directed** - алгоритм с силами притяжения/отталкивания
3. **Hierarchical** - иерархическое расположение на основе зависимостей (по умолчанию)

**Интеграция:**
- Кнопка "Auto Arrange" в SchemaDiagram компоненте
- Автоматическая расстановка всех таблиц с учетом связей
- Сохранение позиций в конфигурации

### ✅ Валидация схемы
**Файлы:**
- `src/utils/schemaValidation.ts` - валидация схемы

**Проверки:**
- Валидация имен таблиц и колонок
- Проверка типов данных
- Проверка первичных ключей
- Валидация внешних ключей и связей
- Проверка циклических зависимостей
- Проверка совместимости типов в связях

**Интеграция:**
- Кнопка "Validate Schema" в PostgreSQLConfigAdvanced
- Диалог с отображением ошибок и предупреждений
- Разделение на ошибки (errors) и предупреждения (warnings)

### ✅ Экспорт изображений
**Зависимости:**
- `html-to-image` - установлено

**Форматы:**
- PNG - высокое качество, прозрачный фон
- JPEG - сжатие, белый фон

**Интеграция:**
- Опции "Export PNG" и "Export JPEG" в dropdown меню Export
- Автоматическое скачивание файлов
- Высокое разрешение (pixelRatio: 2)

### ⚠️ Комментарии к таблицам и полям
**Статус:** Частично реализовано

**Что сделано:**
- Добавлены поля `comment` в типы `PostgreSQLTable` и `PostgreSQLColumn`
- Типы обновлены в `src/core/postgresql/types.ts`

**Что осталось:**
- Добавить UI для редактирования комментариев в таблицах
- Добавить UI для редактирования комментариев в полях
- Парсинг COMMENT ON TABLE/COLUMN в импорте SQL
- Экспорт COMMENT ON TABLE/COLUMN в SQL
- Отображение комментариев в диаграмме (tooltip или под таблицей)

## Использование новых функций

### Автоматическое расположение
1. Откройте таб "Schema Diagram"
2. Нажмите кнопку "Auto Arrange" (иконка сетки) в панели управления
3. Таблицы автоматически расположатся с учетом связей

### Валидация схемы
1. Откройте таб "Schema Diagram"
2. Нажмите кнопку "Validate Schema"
3. Просмотрите результаты валидации в диалоге
4. Исправьте ошибки и предупреждения

### Экспорт изображений
1. Откройте таб "Schema Diagram"
2. Нажмите кнопку "Export"
3. Выберите "Export PNG" или "Export JPEG"
4. Файл автоматически скачается
